дипсик, ты намутил дури для среднестатистических людей. Мои собеседники часто сами талантливы очень и среднестатистическая дурь не нужна. нужна одноранговая система где каждый клиент системы имеет репозиторий в его local storage и общую person node для его кучи устройств. в гитхаб коммитах хранится только костяк системы и общение там сохранять не  надо вовсе. выложи новые TECH SPEC и CLAUDE.md


...skipped...


что за сигнальный сервер? p2p 1-ранговая сеть, сервера персон хранятся у такой роли как "один из администраторов сети"


Понял. Сигнальный сервер — это костыль для установки P2P-соединения (как на WebRTC), но он не хранит данные. Ты говоришь о другом: о **выделенной роли — хранителе узла (администраторе)**, у которого физически лежит серверная часть персоны. Это не просто помощник для связи, это **доверенный хост для данных**.

Это меняет модель. Это уже не pure P2P, где все равны. Это **гибридная модель: P2P-связь между клиентами, но данные персоны (её «ядро») хранятся на выделенном сервере, которым владеет доверенное лицо (администратор сети)**.

Это более реалистично для «жизни», потому что даёт точку постоянства, к которой можно подключиться с любого устройства, не таская с собой seed-фразу.

Давайте перепишем спецификации под эту модель: **P2P-сеть с выделенными серверами-хранителями (Persona Hosts)**.

### **TECHNICAL_SPECIFICATION.md (Версия 4.0: P2P с Host-узлами)**
```markdown
# ТЕХНИЧЕСКОЕ ЗАДАНИЕ: «FAIRYTALE-OF-FAIRYTALES» — СЕТЬ С СЕРВЕРАМИ-ХРАНИТЕЛЯМИ

## 1. НОВАЯ АРХИТЕКТУРНАЯ МОДЕЛЬ: HYBRID P2P
Система состоит из двух типов узлов:
1.  **Persona Host (Сервер-хранитель):** Выделенный сервер, управляемый доверенным администратором. Его задачи:
    *   **Хранить «ядро» данных персоны** (её уникальный ID, криптографические ключи, зашифрованную базу Life States).
    *   **Аутентифицировать** подключения клиентов этой персоны.
    *   **Синхронизировать данные** между всеми устройствами персоны.
    *   **Участвовать в P2-сети:** Принимать и маршрутизировать зашифрованные сообщения для своей персоны от других Host'ов.
2.  **Client (Клиент):** Приложение (Quasar PWA), которое подключается к *своему* Persona Host для загрузки/сохранения данных и устанавливает **прямые P2P-соединения (WebRTC)** с клиентами других персон для обмена сообщениями в реальном времени.

**Аналогия:** Persona Host — это твой **персональный почтовый сервер и хранилище**. Другие Host'ы — почтовые серверы твоих друзей. Клиенты — почтовые программы. Письма (Life States) идут через P2P-каналы между клиентами, но твои архивы лежат на твоём сервере.

## 2. КЛЮЧЕВЫЕ СУЩНОСТИ И ПРОТОКОЛЫ

### 2.1. Persona & Host
*   **Persona ID:** Глобально уникальный идентификатор, связанный с конкретным Host (например, `user@host.domain`).
*   **Persona Host:** Серверное приложение (Node.js/Go). Имеет:
    *   REST/GraphQL API для управления данными персоны (аутентификация по токену).
    *   Базу данных (SQLite/Postgres) для хранения зашифрованных Life States.
    *   WebSocket-сервер для real-time уведомлений клиентов.
    *   Реестр других известных Host'ов в сети.

### 2.2. Life State (Живое Состояние) — РАСШИРЕННАЯ ВЕРСИЯ
*   **Локальная работа:** Клиент создаёт Life State, **шифрует его на клиенте** с помощью ключа, известного только персоне.
*   **Синхронизация с Host:** Клиент отправляет зашифрованный Life State на свой Host через защищённый API. Host сохраняет его в своей БД.
*   **P2P-рассылка:** Если Life State предназначен для других персон (имеет `recipients` список), клиент **устанавливает прямое WebRTC соединение** с клиентами этих персон и передаёт зашифрованное состояние. Получатель может верифицировать отправителя через его Host.

### 2.3. Протокол Discovery и P2P-Связи
1.  **Поиск (Discovery):** Клиент A хочет связаться с Персоной B (`bob@other-host.net`).
    *   Клиент A запрашивает у *своего* Host'а адрес (IP/URL) Host'а для `bob@other-host.net`.
    *   Host A, зная адрес Host B (из конфига или децентрализованного реестра), возвращает его клиенту A.
2.  **Установление P2P (WebRTC Handshake):**
    *   Клиент A связывается с Host B через защищённый канал, запрашивая **сессионный токен** для связи с клиентом B.
    *   Host B аутентифицирует запрос, генерирует токен и уведомляет *свой* клиент B (через WebSocket) о входящем запросе.
    *   Клиенты A и B, используя Host'ы как **сигнальные серверы**, обмениваются SDP/ICE кандидатами и устанавливают **прямое WebRTC Data Channel соединение**.
    *   **Host'ы не видят содержимого трафика P2P-канала.**

## 3. ПОТОК ДАННЫХ: КАК ЭТО РАБОТАЕТ
1.  **Регистрация:** Администратор разворачивает экземпляр **Persona Host** и регистрирует на нём новую Персону, получая `Persona ID`.
2.  **Подключение клиента:** Пользователь вводит свои `Persona ID` и пароль в клиентском приложении. Клиент аутентифицируется на Host, загружает зашифрованную историю Life States.
3.  **Создание Life State:** Пользователь создаёт новое состояние. Клиент шифрует его и:
    *   Отправляет копию на свой Host для сохранения.
    *   Если есть получатели — устанавливает P2P-соединения и отправляет им.
4.  **Получение Life State:** Клиент получает зашифрованное состояние по P2P-каналу, расшифровывает его и сохраняет локально. Также может уведомить свой Host о получении нового состояния (для резервного копирования).

## 4. ТЕХНИЧЕСКИЙ КОНТУР (ЧТО НУЖНО РАЗРАБОТАТЬ)

### 4.1. Persona Host (Серверная часть)
*   **Язык:** Node.js с Express/или Go для производительности.
*   **API:** REST/GraphQL эндпоинты для: регистрации, аутентификации, загрузки/выгрузки данных, управления контактами.
*   **База данных:** SQLite (для простоты развёртывания администратором) или Postgres. **Хранит только шифрованные BLOB'ы.**
*   **WebSocket:** Для real-time уведомлений клиентов (новые P2P-запросы, синхронизация).
*   **Конфигурация:** Файл конфига, где указываются адреса других доверенных Host'ов в сети.

### 4.2. Client (Клиентская часть — Quasar PWA)
*   **Аутентификация и связь с Host:** Модуль для работы с API своего Host'а.
*   **Шифрование:** Весь криптографический функционал (генерация ключей, шифрование/дешифрование Life States) на стороне клиента с использованием Web Crypto API.
*   **P2P-движок:** Управление WebRTC соединениями с другими клиентами.
*   **Локальное кэширование:** IndexedDB для хранения расшифрованных Life States и оффлайн-работы.

### 4.3. Генеральное развёртывание
*   Администратор сети клонирует репозиторий, который содержит **И КОД HOST'а, И КОД КЛИЕНТА**.
*   Запускает серверную часть на своём VPS.
*   Клиентская часть собирается в статику и может хоститься где угодно (на том же VPS, на GitHub Pages). Она **настроена на адрес этого конкретного Host'а**.

## 5. ПРЕИМУЩЕСТВА ЭТОЙ МОДЕЛИ
*   **Мультиустройственность:** Данные доступны с любого устройства, знающего `Persona ID` и пароль.
*   **Административный контроль:** Доверенное лицо обеспечивает доступность и резервное копирование данных своих пользователей.
*   **Конфиденциальность:** Данные шифруются на клиенте. Host и другие участники P2P-сети не видят содержимого.
*   **Реалистичность:** Сочетает децентрализованное общение (P2P) с централизованным, но доверенным, хранением.

---
*Архитектура признаёт социальную реальность: есть те, кто берёт на себя роль инфраструктурных хранителей. Система не анонимно-пиринговая, а **сеть доверенных сообществ, построенных вокруг таких хранителей**.*
```

### **CLAUDE.md (Версия 4.0: Для гибридной P2P-сети с Host)**
```markdown
# CLAUDE.md — ИНСТРУКЦИИ ДЛЯ ГИБРИДНОЙ P2P СЕТИ (CLIENT + HOST)

## НОВАЯ ЦЕЛЬ
Ты строишь **две взаимосвязанные части**:
1.  **Persona Host (Server):** Сервер-хранитель, на котором живут данные персон. Один развёрнутый экземпляр образует **«узел сети»**, которым управляет администратор.
2.  **Client (Quasar PWA):** Тонкое клиентское приложение, которое подключается к Host, работает с данными и устанавливает P2P-соединения с другими клиентами.

Это **НЕ** одноранговая сеть равных узлов. Это **сеть доверенных Host-узлов**, между клиентами которых устанавливаются P2P-каналы.

## ТЕХНИЧЕСКИЙ СТЕК (РАЗДЕЛЁННЫЙ)

### Для Persona Host (Backend)
*   **Язык:** **Node.js + Express** (быстрее для прототипа) или **Go + Fiber** (эффективнее). Выбери Node.js для скорости.
*   **База данных:** **SQLite** (встроенная, не требует отдельной установки для администратора).
*   **Аутентификация:** JWT-токены.
*   **WebSocket:** Библиотека `ws` для real-time уведомлений клиентов.
*   **Шифрование на сервере:** **НЕТ**. Сервер получает и хранит данные уже **зашифрованными клиентом**. Он — «глупое» хранилище.

### Для Client (Frontend)
*   **Quasar (Vue 3) + PWA.**
*   **Pinia** для состояния (личные данные, список контактов, активные P2P-сессии).
*   **LocalForage/IndexedDB** для локального кэша расшифрованных Life States.
*   **Web Crypto API** для ВСЕХ криптоопераций: генерация ключевой пары персоны, шифрование/дешифрование Life States, подпись сообщений.
*   **WebRTC** (`RTCPeerConnection`, `RTCDataChannel`) для установки прямых P2P-соединений с другими клиентами.

## ПЛАН РАЗРАБОТКИ: СНАЧАЛА HOST, ПОТОМ КЛИЕНТ

### ФАЗА 1: PERSONA HOST (СЕРВЕР) (3-4 ДНЯ)
**Создай директорию `/server` в репозитории.**
1.  **Базовая структура Node.js:**
    ```bash
    /server
    ├── package.json
    ├── index.js (главный файл)
    ├── config.js (конфиг: порт, путь к SQLite, секретный ключ JWT)
    ├── database.js (инициализация SQLite + схемы)
    ├── api/ (REST эндпоинты)
    ├── websocket/ (логика WebSocket)
    └── utils/ (утилиты)
    ```
2.  **Схема БД SQLite (`database.js`):**
    ```sql
    -- personas: основные данные персоны
    CREATE TABLE personas (
        id TEXT PRIMARY KEY, -- например, uuid или 'name@host'
        encrypted_private_key TEXT, -- приватный ключ, зашифрованный мастер-паролем
        public_key TEXT,
        created_at DATETIME
    );
    -- life_states: хранилище состояний
    CREATE TABLE life_states (
        id TEXT PRIMARY KEY,
        persona_id TEXT,
        encrypted_data BLOB, -- Данные, зашифрованные на клиенте
        type TEXT,
        timestamp DATETIME,
        FOREIGN KEY (persona_id) REFERENCES personas(id)
    );
    -- contacts: реестр доверенных контактов (другие Host'ы)
    CREATE TABLE contacts (
        host_url TEXT PRIMARY KEY, -- Адрес другого Host'а
        host_public_key TEXT -- Для верификации
    );
    ```
3.  **Реализуй ключевые REST эндпоинты (`api/auth.js`, `api/data.js`):**
    *   `POST /api/register` — регистрация новой персоны (принимает публичный ключ, создаёт запись).
    *   `POST /api/login` — аутентификация (возвращает JWT токен).
    *   `POST /api/life-states` — загрузка нового зашифрованного Life State.
    *   `GET /api/life-states` — получение списка/пакета Life States для синхронизации.
    *   `GET /api/discovery/:persona_id` — эндпоинт для других Host'ов/клиентов, чтобы узнать адрес Host'а для данной персоны.

### ФАЗА 2: CLIENT — АУТЕНТИФИКАЦИЯ И БАЗОВЫЙ РАБОЧИЙ ЦИКЛ (3-4 ДНЯ)
1.  **Настрой Quasar проект** в корне или в директории `/client`. Настрой `quasar.config.js` для PWA.
2.  **Создай сервис для работы с API Host'а (`services/host-api.js`):**
    *   Функции: `register()`, `login()`, `syncLifeStates()`.
    *   Сохраняй JWT токен в безопасное хранилище.
3.  **Создай модуль криптографии (`services/crypto.js`):**
    *   `generateKeyPair()` — генерация пары Ed25519/ECDSA.
    *   `encryptData(text, key)` — шифрование Life State (используй симметричный алгоритм, например, AES-GCM, ключ которого выводится из мастер-пароля).
    *   `decryptData(encryptedBlob, key)` — расшифровка.
4.  **Интегрируй в интерфейс:**
    *   Страница регистрации/входа (запрос к Host).
    *   После входа — главный экран с формой создания Life State.
    *   При создании: шифруй данные -> отправляй на Host через `host-api.js`.

### ФАЗА 3: CLIENT — P2P ENGINE (WEBRTC) (4-5 ДНЕЙ, СЛОЖНАЯ)
1.  **Создай модуль P2P (`services/p2p-engine.js`).** Он будет использовать Host как **сигнальный сервер**:
    *   Через WebSocket-подключение к Host получать уведомления о входящих запросах на соединение.
    *   Использовать REST API Host'а, чтобы передать SDP offer/answer другому клиенту через *их* Host.
2.  **Реализуй установление соединения:**
    *   Клиент A хочет подключиться к Клиенту B.
    *   A запрашивает у Host B (через REST) «сессию для связи».
    *   Host B создаёт запись, уведомляет Клиент B через WebSocket.
    *   Клиенты A и B, используя Host'ы как ретрансляторы, обмениваются SDP и ICE кандидатами.
    *   Устанавливается `RTCDataChannel`.
3.  **Отправка Life State через P2P:** После установки канала, Клиент A шифрует Life State (уже другим ключом, известным получателю) и отправляет по Data Channel.

### ФАЗА 4: ИНТЕГРАЦИЯ, ТЕСТИРОВАНИЕ, ДЕПЛОЙ
1.  Напиши `docker-compose.yml` для лёгкого развёртывания Host администратором.
2.  Создай подробный `DEPLOY.md` с инструкциями: как запустить Host, как сконфигурировать клиент для его использования.
3.  Настрой сборку клиента и хостинг статики (можно на том же сервере, что и Host, в папке `./client/dist/spa`).

## КРИТИЧЕСКИЕ МОМЕНТЫ ДЛЯ MVP
*   **Шифрование:** В MVP можно упростить: использовать один мастер-пароль для шифрования всех данных. Это неидеально, но рабочий прототип.
*   **P2P Discovery:** В MVP можно жёстко прописать адреса Host'ов в конфиге клиента, минуя сложный распределённый реестр.
*   **Цель MVP:** Администратор разворачивает Host. Два пользователя регистрируются на нём. Они могут через клиенты создавать Life States (которые сохраняются на Host) и отправлять их друг другу через **прямое P2P-соединение**, установленное с помощью Host'а как посредника для сигналинга.

**Это не простая задача. Это создание полноценной микро-платформы.**
```

### 🧩 **Резюме изменений в модели 4.0**

1.  **Роли чётко разделены:** **Администратор** управляет **Persona Host** (сервером). **Пользователи** используют **клиенты**, которые подключаются к Host'у и общаются P2P.
2.  **Данные централизованно-децентрализованы:** Ядро данных лежит на доверенном Host'е (для доступности и бэкапа), но **общение идёт напрямую между клиентами (P2P)**, минуя сервер.
3.  **Практично:** Администратор может развернуть это на своём VPS. Пользователям не нужны seed-фразы для доступа с разных устройств — достаточно логина и пароля к их Host'у.
4.  **Соответствует твоему описанию:** «p2p 1-ранговая сеть, сервера персон хранятся у такой роли как "один из администраторов сети"».

Эта модель сложнее для кодирования, но она **реалистичнее и мощнее** чистого P2P для задачи «жизни в сети». Если эта архитектура тебе резонирует, следующим шагом будет начать её реализацию с Claude Code, начиная с **Фазы 1 (Persona Host)**.


данные должны жить на серверах с технологией MIT SOLID от Тима Бёрнерс-Ли. Перегенерируй TECH SPEC.md и CLAUDE.md


Отлично. SOLID — это именно то, что нужно. Это не просто «ещё одна архитектура». Это **философский и технологический стандарт для децентрализованной веб-экосистемы, где данные принадлежат пользователю**.

Использование SOLID радикально упрощает и очищает архитектуру. Вместо того чтобы строить свой собственный хост, сервер аутентификации и протоколы, мы **опираемся на готовый, открытый стандарт**.

Давайте перепишем всё с нуля, используя SOLID как фундамент.

### **TECHNICAL_SPECIFICATION.md (Версия 5.0: SOLID-центричная)**
```markdown
# ТЕХНИЧЕСКОЕ ЗАДАНИЕ: «FAIRYTALE-OF-FAIRYTALES» — КЛИЕНТ ДЛЯ SOLID-ЭКОСИСТЕМЫ

## 1. ФИЛОСОФСКАЯ ОСНОВА: SOLID
Проект **не создаёт новую децентрализованную сеть**. Он **является клиентом для существующей и развивающейся экосистемы SOLID**.

*   **SOLID (Social Linked Data):** Набор стандартов и технологий, созданных Тимом Бернерсом-Ли. Позволяет пользователям хранить свои данные в **персональных онлайн-хранилищах (POD — Personal Online Datastore)** на сервере по своему выбору.
*   **Владелец данных — пользователь.** Приложение запрашивает доступ к данным в POD пользователя. Пользователь контролирует, какие данные и кому открывать.
*   **Децентрализация через стандарт:** Любой может запустить SOLID-сервер (например, с помощью [CSS (Community Solid Server)](https://github.com/CommunitySolidServer/CommunitySolidServer) или [Inrupt PodSpaces](https://start.inrupt.com/)).

**Наш проект — это не платформа. Это интерфейс, «прослойка смысла», которая работает поверх стандартной SOLID-инфраструктуры.**

## 2. АРХИТЕКТУРА: КАК ЭТО РАБОТАЕТ

```
+---------------------+       +-----------------------+
|   Fairytale Client  | <---> |   SOLID Identity     |
|   (Quasar PWA)      |       |   Provider (e.g.,    |
|                     |       |   inrupt.net)        |
+---------------------+       +-----------------------+
         |  (WebID, OIDC)               |
         |                              |
         v  (REST API + LDP)            v
+---------------------+       +-----------------------+
|   User's SOLID POD  |       |   Friend's SOLID POD  |
|   (данные пользователя)     |   (данные друга)      |
|   hosted anywhere           |   hosted elsewhere    |
+---------------------+       +-----------------------+
```

1.  **Пользователь** имеет **WebID** (например, `https://asmexcaliburwoods.solidcommunity.net/profile/card#me`) и **SOLID POD** (персональное хранилище данных по этому адресу).
2.  **Наше приложение (клиент)** просит пользователя **авторизоваться через его SOLID-провайдера** (например, Inrupt). Это стандартный OIDC-процесс.
3.  После авторизации приложение получает **токен доступа** к POD пользователя.
4.  Приложение читает и записывает данные **непосредственно в POD пользователя**, используя стандартные протоколы (LDP — Linked Data Platform, SPARQL).
5.  Чтобы поделиться данными с другом, приложение запрашивает у пользователя разрешение **дать доступ на запись в определённую папку его POD другу** (по его WebID). После этого друг может читать/писать в эту папку со своего клиента.

## 3. КЛЮЧЕВЫЕ СУЩНОСТИ В ТЕРМИНАХ SOLID

*   **Life State** становится **RDF-ресурсом** в POD пользователя. Например, документ в формате Turtle (`.ttl`) с префиксами и предопределённой онтологией.
*   **Связи между Life States** реализуются как **RDF-триплы** (`<state1> <isRelatedTo> <state2>`).
*   **Совместное пространство (Shared Context)** — это **LDP-контейнер** в POD одного из пользователей, на который у других пользователей есть права на запись. Или это отдельный общий POD.
*   **Профиль и связи** — это уже часть стандарта SOLID через файл `profile/card`.

## 4. ПРЕИМУЩЕСТВА ЭТОГО ПОДХОДА

1.  **Не нужно строить бэкенд.** Вся инфраструктура хранения, аутентификации и авторизации уже существует в виде открытых стандартов и готовых серверов (CSS, NSS).
2.  **Данные действительно принадлежат пользователю.** Он может сменить провайдера POD, отозвать доступ у приложения, использовать несколько разных приложений с одними и теми же данными.
3.  **Встроенная децентрализация.** POD пользователя может находиться на любом SOLID-совместимом сервере.
4.  **Фокус на смысле, а не на инфраструктуре.** Мы можем сконцентрироваться на том, что уникально: на интерфейсе, онтологии «Живых Состояний», алгоритмах визуализации связей.

## 5. ТЕХНИЧЕСКИЙ КОНТУР РАЗРАБОТКИ

### 5.1. Клиентская часть (Всё, что мы разрабатываем)
*   **Фреймворк:** Quasar (Vue 3) PWA.
*   **Библиотеки для работы с SOLID:**
    *   **`solid-client-authn-browser`** — для аутентификации (OIDC).
    *   **`solid-client`** (или `@inrupt/solid-client`) — высокоуровневый API для чтения/записи данных в POD.
    *   **`rdflib`** — для работы с RDF на низком уровне, если понадобится.
*   **Локальный кэш:** IndexedDB (через `localForage`) для оффлайн-работы и скорости.

### 5.2. Что нужно определить (Наша предметная область)
*   **Онтология Fairytale:** Набор RDF-классов и свойств, которые описывают `LifeState`, его типы, связи, контекст. Мы создаём свой маленький словарь (например, с префиксом `ft:`).
*   **Структура хранения в POD:** Где и в каком формате (отдельные файлы `.ttl` или одна большая графовая база) мы храним состояния.
*   **Протокол синхронизации совместных пространств:** Как клиенты узнают об изменениях в общем контейнере (например, через периодический опрос или WebSockets, если сервер POD их поддерживает).

## 6. ПОТОК РАБОТЫ ПРИЛОЖЕНИЯ (MVP)

1.  **Вход:** Пользователь нажимает «Login with SOLID». Перенаправляется на провайдера (например, `solidcommunity.net`), входит там и разрешает приложению доступ к своему POD.
2.  **Загрузка данных:** Приложение загружает из POD пользователя все ресурсы типа `ft:LifeState` и отображает их в интерфейсе.
3.  **Создание состояния:** Пользователь создаёт новое состояние. Приложение формирует RDF-граф и создаёт новый ресурс в POD пользователя (например, в контейнере `/fairytale/states/`).
4.  **Добавление друга:** Пользователь вводит WebID друга. Приложение запрашивает у POD пользователя разрешение дать другу доступ на чтение/запись в общий контейнер.
5.  **Совместная работа:** Друг, авторизовавшись *со своим WebID*, получает доступ к общему контейнеру и видит состояния обоих.

---
*Мы не строим остров. Мы строим корабль, который может плавать в уже существующем океане SOLID, соблюдая его законы (стандарты).*
```

### **CLAUDE.md (Версия 5.0: Для SOLID-клиента)**
```markdown
# CLAUDE.md — ИНСТРУКЦИИ ДЛЯ SOLID-КЛИЕНТА «FAIRYTALE-OF-FAIRYTALES»

## КОРЕННОЕ ОТЛИЧИЕ
Ты больше не строишь серверную часть (Host). Ты строишь **только клиентское веб-приложение**, которое является **полноценным гражданином экосистемы SOLID**. Все данные живут в SOLID POD пользователей.

## СТЕК ТЕХНОЛОГИЙ (КЛИЕНТСКИЙ)
1.  **Quasar (Vue 3) + PWA:** Интерфейс и офлайн-возможности.
2.  **SOLID JavaScript Libraries (Ключевые):**
    *   `@inrupt/solid-client-authn-browser`: Для аутентификации (OIDC flow).
    *   `@inrupt/solid-client`: Основной высокоуровневый API для работы с POD (чтение/запись файлов, контейнеров, ACL).
    *   `@inrupt/vocab-common-rdf`: Стандартные RDF-словари.
3.  **Pinia:** Для управления состоянием приложения (текущий пользователь, загруженные Life States, сессия Solid).
4.  **LocalForage/IndexedDB:** Для локального кэширования данных POD и оффлайн-работы.
5.  **(Опционально) `rdflib`:** Для продвинутой работы с RDF, если `solid-client` будет недостаточно.

## ПЛАН РАЗРАБОТКИ MVP

### ФАЗА 0: НАСТРОЙКА ПРОЕКТА И SOLID-АУТЕНТИФИКАЦИИ (День 1-2)
1.  **Инициализируй Quasar проект.**
2.  **Установи SOLID-библиотеки:**
    ```bash
    npm install @inrupt/solid-client-authn-browser @inrupt/solid-client @inrupt/vocab-common-rdf
    ```
3.  **Создай сервис для работы с SOLID (`services/solid-service.js`):**
    *   Инициализация `Session` объекта из `solid-client-authn-browser`.
    *   Функции `login(identityProvider)`, `logout()`, `handleIncomingRedirect()` (для обработки возврата с провайдера).
    *   Конфигурация: Укажи несколько популярных провайдеров по умолчанию для выбора (`solidcommunity.net`, `inrupt.net`).
4.  **Создай компонент/страницу входа (`pages/LoginPage.vue`):**
    *   Кнопки «Login with SolidCommunity», «Login with Inrupt».
    *   При нажатии — вызов `solid-service.login()`.

### ФАЗА 1: ОПРЕДЕЛЕНИЕ ОНТОЛОГИИ И ЧТЕНИЕ ДАННЫХ (День 3-4)
1.  **Определи онтологию Fairytale.** Создай файл `ontologies/fairytale.js`. Пример:
    ```javascript
    import { RDF } from '@inrupt/vocab-common-rdf';
    // Наша собственная онтология (пока используем уникальный базовый URL)
    export const FT = {
      NAMESPACE: 'http://fairytale.of/ontology#',
      LifeState: 'LifeState',
      content: 'content',
      stateType: 'stateType', // thought, observation, decision...
      relatesTo: 'relatesTo'
    };
    // Функция для получения полного IRI
    export function asIri(term) { return FT.NAMESPACE + term; }
    ```
2.  **Расширь `solid-service.js`:** Добавь функции для работы с данными:
    *   `getOrCreateFairytaleContainer(session)`: Находит или создаёт в корне POD пользователя контейнер `/fairytale/` и внутри него `/fairytale/states/`.
    *   `getLifeStates(session)`: Загружает все ресурсы из контейнера `/fairytale/states/`, фильтрует те, у которых `rdf:type` равен нашему `ft:LifeState`, и парсит их в простые JS-объекты для Pinia.
3.  **Создай главную страницу (`pages/IndexPage.vue`):**
    *   При монтировании проверяй сессию, если пользователь авторизован — вызывай `getLifeStates()` и отображай их в виде списка.

### ФАЗА 2: СОЗДАНИЕ И ЗАПИСЬ LIFE STATE (День 5-6)
1.  **Создай компонент создания состояния (`components/CreateStateForm.vue`).**
2.  **В `solid-service.js` добавь функцию `createLifeState(session, data)`:** Она должна:
    *   Сгенерировать уникальный ID для нового ресурса (например, `state_<timestamp>.ttl`).
    *   Создать RDF-граф, используя `solid-client`:
        ```javascript
        import { createThing, addUrl, addStringNoLocale, saveSolidDatasetAt, setThing } from '@inrupt/solid-client';
        let newThing = createThing({ name: stateId });
        newThing = addUrl(newThing, RDF.type, asIri(FT.LifeState));
        newThing = addStringNoLocale(newThing, asIri(FT.content), data.text);
        // ... остальные поля
        ```
    *   Сохранить новый ресурс в POD пользователя по адресу `/fairytale/states/{stateId}.ttl`.
3.  **Интегрируй форму в главную страницу.** После успешного сохранения обновляй список состояний.

### ФАЗА 3: ОБЩИЕ ПРОСТРАНСТВА И РАЗРЕШЕНИЯ (SHARING) (День 7-8, Сложная)
1.  **Добавь функционал управления доступом (ACL):**
    *   В `solid-service.js` создай функцию `shareContainerWithFriend(session, friendWebId, containerUrl)`.
    *   Используй `solid-client` API для установки ACL: дать `friendWebId` права на `READ` и `APPEND` (или `WRITE`) к указанному контейнеру.
2.  **Создай интерфейс:** Поле для ввода WebID друга и кнопку «Пригласить в общее пространство».
3.  **Логика совместной работы:** MVP может работать с **одним общим контейнером** (например, `/fairytale/shared/`). Приглашённые друзья будут читать и писать туда.

### ФАЗА 4: ЛОКАЛЬНЫЙ КЭШ И ОФФЛАЙН-РЕЖИМ (День 9-10)
1.  **Интегрируй LocalForage.**
2.  **Модифицируй `getLifeStates`:** При успешной загрузке из сети — сохраняй сырые данные в IndexedDB.
3.  **При запуске приложения:** Сначала показывай данные из кэша, затем пытайся синхронизироваться с POD в фоне.
4.  **При создании состояния оффлайн:** Сохраняй его в локальную очередь на отправку. При появлении сети — синхронизируй.

### ФАЗА 5: СБОРКА, ТЕСТИРОВАНИЕ, ДОКУМЕНТАЦИЯ
1.  Протестируй с реальными POD на `solidcommunity.net`.
2.  Напиши понятный `README.md`, объясняющий: «Это клиент для SOLID. Вам нужен SOLID-аккаунт (например, на solidcommunity.net).»
3.  Настрой деплой статики на GitHub Pages.

## КРИТИЧЕСКИЕ МОМЕНТЫ И УПРОЩЕНИЯ ДЛЯ MVP
*   **Хранение:** В MVP храни каждое Life State как **отдельный файл `.ttl`** в контейнере. Это проще для начала, чем работать с одним большим графом.
*   **Поиск/Фильтрация:** Для MVP достаточно простого перебора всех файлов в контейнере. Оптимизации — позже.
*   **Уведомления об изменениях:** В MVP можно обходиться периодическим опросом (polling) общего контейнера раз в N секунд. Настоящие WebSockets (Solid Notifications) — сложнее.
*   **Цель MVP:** Пользователь с SOLID-аккаунтом может зайти в приложение, увидеть свои старые состояния (если есть), создать новое, пригласить друга по WebID и увидеть состояние, созданное другом в общем контейнере.

**Ты строишь не систему. Ты строишь проводник в систему, которая уже существует.**
```

### 🚀 **Что это меняет для тебя**

1.  **Нет необходимости администрировать сервер** (если только ты не хочешь запустить свой экземпляр SOLID-сервера для своего круга).
2.  **Ты и твои собеседники можете использовать уже существующие SOLID-провайдеры** (например, зарегистрироваться на `solidcommunity.net`). Данные будут там.
3.  **Разработка фокусируется на самом важном:** на интерфейсе, логике работы с «Живыми Состояниями» и их визуализации, а не на изобретении колеса (аутентификации, хранения, протоколов обмена).
4.  **Система от рождения совместима с экосистемой децентрализованного веба.** Это не изолированный остров.

Это самый чистый и соответствующий духу времени подход. Если он тебе подходит, следующим шагом будет начать с **Фазы 0**, установив зависимости и настроив аутентификацию.


переименуй LifeState в LivelyRealm и перегенерируй TECH SPEC.md и CLAUDE.md


Готово. «LivelyRealm» — это мощное название. Оно переводит фокус с отдельного «состояния» на **живую, дышащую сферу смысла и данных, управляемую пользователем**.

Вот переработанные документы.

### **TECHNICAL_SPECIFICATION.md (Версия 6.0: LivelyRealm в SOLID)**
```markdown
# ТЕХНИЧЕСКОЕ ЗАДАНИЕ: «FAIRYTALE-OF-FAIRYTALES» — КЛИЕНТ ДЛЯ УПРАВЛЕНИЯ LIVELYREALM В SOLID

## 1. КОНЦЕПЦИЯ: LIVELYREALM КАК ОСНОВНАЯ ЕДИНИЦА
Проект является **клиентом для управления LivelyRealm'ами в экосистеме SOLID**.

*   **LivelyRealm (Живое Царство):** Это основная, самодостаточная единица организации смысла и данных. Это не просто «пост» или «состояние». Это **контейнер для связанных мыслей, решений, наблюдений и артефактов, объединённых общим контекстом, целью или временным промежутком**. Примеры LivelyRealm'а: «Проект Квартира», «Виртуальное Государство — Конституция», «Диалог с [Имя] за май 2024», «Сборник идей о биосфере».
*   **SOLID POD как почва:** Каждый LivelyRealm живёт внутри **SOLID POD пользователя** как набор связанных RDF-ресурсов в выделенном контейнере. SOLID обеспечивает хранение, безопасность, принадлежность и механизмы общего доступа.
*   **Клиент как садовник:** Наше приложение — это инструмент для **создания, культивации, соединения и навигации по LivelyRealm'ам** в плодородной почве SOLID POD'а.

## 2. АРХИТЕКТУРА: ИЕРАРХИЯ В SOLID

```
SOLID POD пользователя (https://user.solid.pod/)
│
├── /fairytale/ (Корневой контейнер приложения)
│   │
│   ├── /realms/ (Контейнер для всех LivelyRealm'ов)
│   │   ├── /realm-project-apartment/ (Один LivelyRealm)
│   │   │   ├── index.ttl (Метаданные Realm'а: название, описание, участники)
│   │   │   ├── entries/ (Контейнер для записей внутри Realm'а)
│   │   │   │   ├── entry-2024-05-27-1.ttl
│   │   │   │   └── entry-2024-05-28-1.ttl
│   │   │   └── resources/ (Контейнер для прикреплённых файлов, ссылок)
│   │   │
│   │   └── /realm-virtual-state/ (Другой LivelyRealm)
│   │
│   └── /ontology/ (Контейнер для онтологии приложения, общий для всех)
│       └── fairytale.ttl
│
└── /profile/card#me (WebID профиль пользователя)
```

## 3. КЛЮЧЕВЫЕ СУЩНОСТИ И ИХ СВОЙСТВА (RDF)

### 3.1. LivelyRealm (Класс `ft:Realm`)
*   **`ft:name`** (xsd:string): Название Realm'а («Стройка Крепости»).
*   **`ft:description`** (xsd:string): Краткое описание.
*   **`ft:created`** (xsd:dateTime): Дата создания.
*   **`ft:contributor`** (WebID): Ссылка на WebID участников (управляется через SOLID ACL).
*   **`ft:containsEntry`** (URL): Ссылка на записи внутри Realm'а.

### 3.2. Realm Entry (Класс `ft:Entry`) — атомарная запись внутри Realm'а
*   **`ft:entryContent`** (xsd:string): Основное содержание (текст).
*   **`ft:entryType`** (ft:EntryType): Тип: `idea`, `observation`, `decision`, `question`, `milestone`.
*   **`ft:created`** (xsd:dateTime).
*   **`ft:inReplyTo`** (URL): Ссылка на другую запись (для диалога или ветвления).
*   **`ft:tag`** (xsd:string): Произвольные теги для связи.

### 3.3. Связи
*   **Связь Realm'ов:** Realm'ы могут связываться через `ft:relatedRealm`.
*   **Граф записей:** Записи внутри Realm'а образуют граф через `ft:inReplyTo` и общие теги.

## 4. ПОТОК РАБОТЫ ПОЛЬЗОВАТЕЛЯ

1.  **Вход в SOLID POD.** (Стандартная OIDC-авторизация).
2.  **Обзор LivelyRealm'ов.** Главный экран — это список/сеть всех Realm'ов пользователя.
3.  **Создание нового LivelyRealm'а.** Пользователь задаёт имя, описание. Система создаёт структуру папок в POD.
4.  **Работа внутри LivelyRealm'а.** Пользователь попадает в интерфейс, сфокусированный на конкретном Realm'е. Здесь он создаёт, редактирует, связывает записи (`Entry`).
5.  **Приглашение со-творцов.** В настройках Realm'а пользователь вводит WebID друга и выдает ему права на контейнер этого Realm'а через SOLID ACL. Друг теперь может зайти в этот же Realm со своего аккаунта и добавлять свои записи.
6.  **Навигация по связям.** Пользователь может видеть, как Realm'ы и записи связаны между собой, переходить по этим связям.

## 5. ПРЕИМУЩЕСТВА ДАННОЙ МОДЕЛИ
*   **Естественная организация:** Соответствует тому, как мы мыслим — проектами, темами, диалогами.
*   **Изоляция контекстов:** Работа над «Квартирой» не смешивается с «Виртуальным Государством».
*   **Гибкое соучастие:** Можно приглашать разных людей в разные Realm'ы с разными правами.
*   **Мощность SOLID:** Наследование всех преимуществ SOLID (децентрализация, контроль данных, стандарты).

## 6. ТЕХНИЧЕСКИЙ КОНТУР КЛИЕНТА
*   **Фреймворк:** Quasar (Vue 3) PWA.
*   **SOLID SDK:** `@inrupt/solid-client-authn-browser`, `@inrupt/solid-client`.
*   **Управление состоянием:** Pinia (храним загруженные Realm'ы, текущий Realm, его записи).
*   **Визуализация графов:** Библиотека типа `vue3-force-graph` или `cytoscape.js` для отображения связей между Realm'ами и записями.
*   **Локальный кэш:** IndexedDB для оффлайн-доступа к недавно просмотренным Realm'ам.

---
*Приложение не создаёт данные. Оно создаёт **LivelyRealm'ы** — структурированные пространства внутри данных пользователя, где эти данные обретают смысл и связность.*
```

### **CLAUDE.md (Версия 6.0: Для управления LivelyRealm'ами)**
```markdown
# CLAUDE.md — ИНСТРУКЦИИ ДЛЯ КЛИЕНТА УПРАВЛЕНИЯ LIVELYREALM

## СМЕНА ПАРАДИГМЫ
Ты строишь **менеджер контекстов (LivelyRealm'ов)**, а не линейный лентой или блокнот. Основная сущность — **Realm**, внутри которого находятся **Entries**.

## СТЕК ТЕХНОЛОГИЙ (БЕЗ ИЗМЕНЕНИЙ, НО С НОВОЙ ЛОГИКОЙ)
*   **Quasar (Vue 3) + PWA**
*   **SOLID SDK:** `@inrupt/solid-client-authn-browser`, `@inrupt/solid-client`
*   **Pinia** — для состояния: `sessionStore`, `realmsStore` (список Realm'ов), `currentRealmStore` (выбранный Realm и его записи).
*   **IndexedDB/LocalForage** — для кэширования структуры Realm'ов.

## ПОШАГОВЫЙ ПЛАН РАЗРАБОТКИ (MVP)

### ЭТАП 0: АУТЕНТИФИКАЦИЯ И СКЕЛЕТ ПРИЛОЖЕНИЯ (День 1-2)
1.  Инициализируй Quasar проект, установи SOLID-библиотеки.
2.  Создай сервис `services/solid-auth.js` с базовыми функциями `login()`, `logout()`, `getSession()`.
3.  Создай макет приложения:
    *   `layouts/MainLayout.vue` — с областью для навигации и основного контента.
    *   `pages/LoginPage.vue` — страница входа.
    *   `pages/RealmsDashboardPage.vue` — будущая главная страница со списком Realm'ов.

### ЭТАП 1: ОПРЕДЕЛЕНИЕ ОНТОЛОГИИ И БАЗОВЫХ СЕРВИСОВ (День 3)
1.  **Создай онтологию.** В `ontologies/fairytale.js` определи константы:
    ```javascript
    export const FT = {
      NAMESPACE: 'http://fairytale.of/ontology#',
      // Классы
      Realm: 'Realm',
      Entry: 'Entry',
      // Свойства для Realm
      realmName: 'realmName',
      realmDescription: 'realmDescription',
      containsEntry: 'containsEntry',
      relatedRealm: 'relatedRealm',
      // Свойства для Entry
      entryContent: 'entryContent',
      entryType: 'entryType', // Используй предопределённые типы: 'idea', 'note', 'decision', 'question'
      inReplyTo: 'inReplyTo',
      tag: 'tag'
    };
    ```
2.  **Создай сервис для работы с Realm'ами (`services/realm-service.js`).** Ключевые функции:
    *   `ensureAppContainer(session)`: Создаёт корневой `/fairytale/` и дочерние `/realms/`, `/ontology/` в POD, если их нет.
    *   `getAllRealms(session)`: Читает контейнер `/realms/`, находит все подконтейнеры (каждый — это Realm), читает их `index.ttl` и возвращает массив объектов с метаданными.
    *   `createRealm(session, name, description)`: Создаёт новую папку в `/realms/` (например, `realm-<uuid>`), внутри неё — `index.ttl` с метаданными и пустые контейнеры `entries/`, `resources/`.

### ЭТАП 2: ИНТЕРФЕЙС ДЛЯ REALM'ОВ (День 4-5)
1.  **Страница `RealmsDashboardPage.vue`:**
    *   Используй `getAllRealms()` при загрузке.
    *   Отобрази список Realm'ов (карточки или список).
    *   Кнопка «+ Создать новый Realm» — открывает модальное окно, которое вызывает `createRealm()`.
2.  **Страница `RealmDetailPage.vue`:**
    *   Принимает параметр `realmId` (имя папки Realm'а).
    *   При входе загружает `index.ttl` этого Realm'а и все записи из `/entries/`.
    *   Отображает название, описание и **ленту записей (Entries)** этого Realm'а.

### ЭТАП 3: РАБОТА С ENTRY ВНУТРИ REALM'А (День 6-7)
1.  **Расширь `realm-service.js`** функциями для работы с записями:
    *   `getEntriesForRealm(session, realmContainerUrl)`: Читает все `.ttl` файлы в `{realmContainerUrl}entries/` и парсит их.
    *   `createEntry(session, realmContainerUrl, entryData)`: Создаёт новый файл `.ttl` в контейнере entries, формирует RDF-граф из данных.
2.  **В `RealmDetailPage.vue` добавь:**
    *   Форму для создания новой Entry (поле ввода, выбор типа `entryType`).
    *   Отображение существующих записей в виде ленты (с сортировкой по дате).
    *   У каждой записи — кнопка «Ответить», которая предзаполняет форму и устанавливает `inReplyTo`.

### ЭТАП 4: ОБЩИЙ ДОСТУП (SHARING) И НАВИГАЦИЯ (День 8-9)
1.  **Добавь управление доступом.** В `realm-service.js` создай `shareRealm(session, realmContainerUrl, friendWebId, permissions)`.
    *   Используй `setActorAccess` из `solid-client`, чтобы дать `friendWebId` права (`READ`, `APPEND`, `WRITE`) на весь контейнер Realm'а.
2.  **Добавь UI:** На странице деталей Realm'а вкладка «Участники» с полем для ввода WebID и кнопкой «Пригласить».
3.  **Базовая навигация по связям:** Записи с `inReplyTo` могут быть кликабельны для перехода к родительской записи. Можно добавить простой вид «дерева обсуждения».

### ЭТАП 5: ЛОКАЛЬНЫЙ КЭШ И ФИНАЛИЗАЦИЯ (День 10)
1.  Интегрируй LocalForage.
2.  Кэшируй метаданные Realm'ов и последние просмотренные записи.
3.  Настрой деплой на GitHub Pages.
4.  Напиши `README.md`, объясняющий, что для работы нужен SOLID-аккаунт и что такое LivelyRealm.

## УПРОЩЕНИЯ ДЛЯ MVP
*   **Поиск:** Не реализовывать. Достаточно навигации по списку Realm'ов и ленте внутри Realm'а.
*   **Сложные графы:** Не визуализировать весь граф связей между Realm'ами. Достаточно отображать `relatedRealm` как простой список ссылок.
*   **Типы Entry:** Жёсткий набор (`idea`, `note`, `decision`, `question`). Не делать настраиваемым.
*   **Уведомления:** Без real-time. Обновление списка записей по кнопке или таймеру.

## ЦЕЛЬ MVP
Пользователь с SOLID POD должен иметь возможность:
1.  Войти в приложение.
2.  Увидеть список своих LivelyRealm'ов (изначально пустой).
3.  Создать новый LivelyRealm «Моя Крепость».
4.  Внутри него создать несколько записей (`Entry`) разных типов, отвечая самому себе.
5.  Пригласить друга (по WebID) в этот Realm.
6.  **Друг, войдя со своего аккаунта, должен увидеть тот же Realm и добавить в него свою запись.**

**Это будет момент, когда два LivelyRealm'а (личные миры пользователей) пересекутся в одном общем цифровом пространстве, построенном на SOLID.**
```

### 🎯 **Что принципиально нового в модели LivelyRealm**

1.  **Иерархия и контекст:** `LivelyRealm` — это **контейнер более высокого порядка**. Это не просто запись, а **пространство для работы**, что идеально ложится на твои исходные концепции «садиков», «виртуального государства», «проекта квартиры». Каждый из них может быть отдельным LivelyRealm'ом.
2.  **Структура внутри POD** становится логичной и человеко-читаемой: папки `realms/`, а внутри — папка для каждого проекта/темы.
3.  **Совместная работа** происходит на уровне целого Realm'а, что интуитивно понятнее, чем обмен отдельными сообщениями.
4.  **Масштабируемость:** Можно начать с одного Realm'а, а затем создать сеть связанных между собой Realm'ов, отражая сложность твоих замыслов.

Теперь у тебя есть архитектура, которая:
*   Использует готовую, этичную инфраструктуру (SOLID).
*   Оперирует правильными абстракциями (LivelyRealm, Entry).
*   Позволяет строить сложные, связанные структуры смысла.
*   Даёт полный контроль над данными.

Следующий шаг — начать кодить с **ЭТАПА 0**. Готов создать репозиторий и дать старт?


ты не строй структуры смысла, а создай простую социальную сеть с сущностями ProtectedLivelyRealm, метафора: бомбоубежище. позже мы на quasar сделаем i2p-коннектор и люди из i2p сети смогут в этом quasar-приложении жить и общаться. Позже спортируем quasar приложение на Qt


Понял. От сложных онтологий и SOLID — к **практичному, минималистичному и защищённому прототипу социальной сети-бомбоубежища с заделом на i2p и десктоп**. Это правильный тактический ход.

Вот техспецификация и инструкция для реализации **ProtectedLivelyRealm** как простой социальной сети, которая может эволюционировать.

### **TECHNICAL_SPECIFICATION.md (Версия 7.0: ProtectedLivelyRealm как бомбоубежище)**
```markdown
# ТЕХНИЧЕСКОЕ ЗАДАНИЕ: «FAIRYTALE-OF-FAIRYTALES» / «BUNKER-NET»
## Социальная сеть на базе ProtectedLivelyRealm с фокусом на приватность и будущую интеграцию с i2p.

### 1. КОНЦЕПЦИЯ: «БОМБОУБЕЖИЩЕ» (PROTECTED LIVELY REALM)
Это простая социальная сеть, где основная единица — **«Бомбоубежище» (ProtectedLivelyRealm)**.
*   **Убежище:** Это закрытая группа/комната для общения. Текстовый чат + общие файлы. Доступ — только по приглашению владельца.
*   **Приватность по умолчанию:** Нет публичных профилей, лент, алгоритмов. Всё происходит внутри убежищ.
*   **Архитектура для будущего:** Клиентское приложение проектируется с учётом последующей **интеграции с i2p (Invisible Internet Project)** для анонимного сетевого слоя и **портирования на Qt для нативных десктоп-приложений**.

### 2. СУЩНОСТИ И ФУНКЦИОНАЛ (MVP)

#### 2.1. Пользователь (User)
*   **Локальный профиль:** Имя (ник), аватар (опционально). **Не привязан к email/телефону.** Профиль существует только в localStorage данного браузера/устройства.
*   **Идентификация в сети:** В MVP — **никакая**. Пользователь известен только внутри тех «убежищ», куда его добавили, под своим ником. В будущем — через i2p-адрес (`.b32.i2p`).

#### 2.2. ProtectedLivelyRealm (Убежище / Бомбоубежище)
*   **Создаётся пользователем.** Указывается название, описание.
*   **Управление доступом:** Владелец (создатель) может **генерировать инвайт-ссылку** (с одноразовым токеном). Тот, у кого есть ссылка, может присоединиться.
*   **Контент внутри:**
    1.  **Хронологический чат:** Сообщения от участников (текст, дата, автор).
    2.  **Общие файлы/медиа:** Возможность загрузить файл (изображение, документ). Файлы хранятся на сервере и доступны участникам убежища.

#### 2.3. Серверная часть (Backend)
*   **Назначение:** MVP требует простой централизованный сервер для синхронизации сообщений и файлов между участниками убежища.
*   **Технология:** Node.js + Express + Socket.io (для real-time чата) + база (SQLite/Postgres для хранения сообщений и метаданных файлов).
*   **Важно:** Сервер — **временная мера (костыль)** для MVP. Его API проектируется так, чтобы в будущем его можно было заменить на **распределённую сеть поверх i2p**.

### 3. АРХИТЕКТУРА КЛИЕНТА (QUASAR PWA)

#### 3.1. Текущий стек (MVP)
*   **Фреймворк:** Quasar (Vue 3) в режиме PWA (для ощущения нативного приложения).
*   **Состояние:** Pinia (менеджмент пользователя, список убежищ, активный чат).
*   **Связь с сервером:**
    *   **REST API:** Для операций с убежищами (создание, получение списка, инвайты), загрузки файлов.
    *   **WebSocket (Socket.io):** Для real-time чата внутри убежища.
*   **Локальное хранилище:** IndexedDB (кеш сообщений, профиль пользователя).

#### 3.2. Задел на будущее: i2p и Qt
*   **i2p-коннектор:** Будущий модуль `services/i2p-connector.js`, который заменит прямой REST/WebSocket вызов к центральному серверу на общение через **i2p скрытые сервисы (eepsites)**. API сервера должен быть к этому готов (например, использовать JSON over HTTP/WebSockets).
*   **Портирование на Qt:** Структура Quasar-проекта должна быть чистой. Использовать **Capacitor** для сборки под Desktop не рекомендуется, если нужен нативный Qt. Вместо этого: выделить бизнес-логику (Pinia-стора, сервисы) в отдельный слой, который позже можно будет переписать на C++/QML, оставив Vue-компоненты только для веб-версии.

### 4. ПОТОК РАБОТЫ ПОЛЬЗОВАТЕЛЯ (MVP)

1.  **Первый запуск:** Приложение предлагает ввести **никнейм** и выбирать аватар. Эти данные сохраняются в `localStorage`. Серверу они не отправляются до создания/входа в убежище.
2.  **Главный экран:** Список «Мои убежища». Кнопка «Создать новое убежище».
3.  **Создание убежища:** Ввод названия. Сервер создаёт запись и возвращает `realmId` и **ссылку-приглашение**. Этой ссылкой можно поделиться.
4.  **Вход по инвайту:** Пользователь переходит по ссылке (или вводит код). Приложение отправляет на сервер никнейм пользователя и токен инвайта. Сервер добавляет пользователя в участники.
5.  **Чат внутри убежища:** Интерфейс, похожий на Telegram/Discord. Слева — список участников (по никам), основная область — хронология сообщений. Внизу — поле ввода.
6.  **Обмен файлами:** Кнопка «Прикрепить файл» в поле ввода чата.

### 5. ПЛАН РАЗВИТИЯ ПОСЛЕ MVP
1.  **Этап 1 (MVP):** Централизованный сервер, Quasar PWA.
2.  **Этап 2 (i2p):** Разработка i2p-коннектора. Замена сетевого слоя клиента. Настройка i2p-скрытого сервиса для серверной части.
3.  **Этап 3 (Qt):** Анализ кодовой базы, выделение логики. Создание Qt-приложения с QML-интерфейсом, использующего тот же i2p-сетевой слой.

---
*Мы строим не «ещё одну соцсеть», а **прототип коммуникационной ячейки (бомбоубежища)**, которая может эволюционировать в сторону полной децентрализации и анонимности (i2p) и нативных платформ (Qt).*
```

### **CLAUDE.md (Версия 7.0: Для социальной сети-бомбоубежища)**
```markdown
# CLAUDE.md — ИНСТРУКЦИИ ДЛЯ «BUNKER-NET» MVP

## ЦЕЛЬ MVP
Создать работающий прототип минималистичной социальной сети с комнатами (ProtectedLivelyRealms), чатом и обменом файлами на централизованном сервере. Заделы под i2p и Qt.

## СТЕК ДЛЯ MVP

### Сервер (Backend) - `/server/`
*   **Runtime:** Node.js
*   **Фреймворк:** Express.js
*   **Real-time:** Socket.io
*   **База данных:** SQLite (для простоты развёртывания)
*   **Хранение файлов:** Локальная файловая система (папка `uploads/`), позже - объектное хранилище.

### Клиент (Frontend) - `/client/` или корень
*   **Фреймворк:** Quasar (Vue 3) в режиме PWA.
*   **Управление состоянием:** Pinia
*   **Связь с сервером:** Axios (для REST), Socket.io-client (для чата).
*   **UI-компоненты:** Используй нативные Quasar компоненты (QCard, QInput, QChatMessage, QAvatar).
*   **Локальное хранилище:** Pinia с persistence в localStorage для профиля.

## ПЛАН РАЗРАБОТКИ MVP

### ФАЗА 0: СЕРВЕР — БАЗОВАЯ СТРУКТУРА И АПИ (2-3 дня)
**Создай директорию `/server`.**

1.  **Инициализация:**
    ```bash
    mkdir server && cd server
    npm init -y
    npm install express socket.io sqlite3 multer cors
    npm install -D nodemon
    ```
2.  **Схема базы данных (`server/database.js`):**
    ```sql
    -- Убежища (Realms)
    CREATE TABLE realms (
        id TEXT PRIMARY KEY, -- UUID
        name TEXT NOT NULL,
        description TEXT,
        owner_nickname TEXT, -- Ник создателя (локальный, для отображения)
        invite_token TEXT UNIQUE, -- Одноразовый токен для вступления
        created_at DATETIME DEFAULT CURRENT_TIMESTAMP
    );
    -- Участники убежищ (многие-ко-многим)
    CREATE TABLE realm_members (
        realm_id TEXT,
        member_nickname TEXT, -- Ник участника
        joined_at DATETIME,
        PRIMARY KEY (realm_id, member_nickname),
        FOREIGN KEY (realm_id) REFERENCES realms(id)
    );
    -- Сообщения
    CREATE TABLE messages (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        realm_id TEXT,
        author_nickname TEXT,
        content TEXT,
        attachment_url TEXT, -- Путь к файлу, если есть
        timestamp DATETIME,
        FOREIGN KEY (realm_id) REFERENCES realms(id)
    );
    ```
3.  **REST API (`server/routes/`):**
    *   `POST /api/realms` — Создать убежище. Тело: `{ name, description, ownerNickname }`. Ответ: `{ realmId, inviteToken }`.
    *   `POST /api/realms/join` — Присоединиться по инвайту. Тело: `{ inviteToken, memberNickname }`. Ответ: `{ realmId, realmName }`.
    *   `GET /api/realms/:realmId/messages?limit=50` — Получить историю сообщений.
    *   `POST /api/upload` — Загрузить файл (используй `multer`). Возвращает `{ fileUrl }`.

4.  **WebSocket (Socket.io) для чата:**
    *   При подключении клиент отправляет событие `join-realm` с `{ realmId, nickname }`.
    *   Сервер добавляет сокет в комнату `realmId`.
    *   Клиент шлёт событие `send-message` с `{ realmId, content, attachmentUrl? }`.
    *   Сервер сохраняет в БД и рассылает `new-message` всем в комнате.

### ФАЗА 1: КЛИЕНТ — ПРОФИЛЬ И ГЛАВНЫЙ ЭКРАН (2 дня)
1.  **Инициализируй Quasar проект** в корне (`/`) или `/client`.
2.  **Создай хранилище Pinia (`stores/user-store.js`):**
    ```javascript
    state: () => ({
        nickname: null,
        avatar: null,
        myRealms: [] // Список объектов { id, name }
    }),
    actions: {
        setProfile({ nickname, avatar }) { ... }, // Сохраняет в localStorage
        loadRealms() { ... } // Делает запрос к GET /api/users/me/realms (его нужно создать)
    }
    ```
3.  **Страница `ProfileSetup.vue`:** Если в сторе нет nickname, показывай её. Поле для ника, выбор аватара. По сохранению — `userStore.setProfile()`.
4.  **Главная страница `HomePage.vue`:** Если профиль есть, показывай список `myRealms` (пока пустой) и кнопку «Создать убежище».

### ФАЗА 2: КЛИЕНТ — РАБОТА С УБЕЖИЩАМИ (3 дня)
1.  **Сервис для API (`services/api.js`):** Обёртки над Axios для вызовов к `/api/realms` и `/api/upload`.
2.  **Модальное окно создания убежища:** Вызывает `api.createRealm()`, получает `inviteToken`. Показывает пользователю **ссылку для приглашения** вида `https://наш-сайт/#/invite/<token>`.
3.  **Страница входа по инвайту (`pages/InvitePage.vue`):**
    *   Читает токен из URL (`$route.params.token`).
    *   Автоматически подставляет текущий nickname и отправляет `api.joinRealm()`.
    *   При успехе — добавляет убежище в `userStore.myRealms` и переходит в него.
4.  **Страница убежища (`pages/RealmPage.vue`):**
    *   Принимает `realmId`. Загружает историю сообщений через API.
    *   **Подключается к WebSocket комнате** этого `realmId`.
    *   Отображает список участников (пока можно просто статичный список из ников в сообщениях) и ленту чата.

### ФАЗА 3: КЛИЕНТ — ЧАТ И ФАЙЛЫ (3 дня)
1.  **Компонент чата (`components/ChatMessage.vue`):** Отображает ник, текст, дату, превью файла.
2.  **Панель ввода сообщения:** Текстовое поле + кнопка «Прикрепить файл». При выборе файла:
    *   Вызывай `api.uploadFile(formData)`.
    *   Получив `fileUrl`, отправляй сообщение через WebSocket с полем `attachmentUrl`.
3.  **Обработка входящих сообщений по WebSocket:** При событии `new-message` добавляй сообщение в массив в сторе текущего убежища.

### ФАЗА 4: ИНТЕГРАЦИЯ И ДЕПЛОЙ (2 дня)
1.  Напиши `docker-compose.yml` для сервера (Node + SQLite).
2.  Собери Quasar PWA (`quasar build`).
3.  Настрой деплой: сервер на VPS, статику клиента — на GitHub Pages или в папку на том же VPS (например, `nginx`).
4.  **Протестируй сценарий:** Создать убежище, присоединиться с другого браузера, обменяться сообщениями и файлом.

## КРИТИЧЕСКИЕ УПРОЩЕНИЯ ДЛЯ MVP
*   **Нет регистрации/паролей.** Вся идентификация — через инвайт-токен и локальный ник.
*   **Нет шифрования сообщений.** Все данные в открытом виде на сервере. (Это осознанный компромисс для скорости MVP, но это **главный пункт для следующей итерации с i2p**).
*   **Управление участниками:** В MVP владелец не может удалять участников, только создавать новые инвайты.
*   **История чата:** Всегда подгружается с сервера, нет оффлайн-режима.

## ЦЕЛЬ MVP
**Два человека в разных браузерах должны иметь возможность:**
1.  Создать/войти в локальный профиль (ник).
2.  Один создаёт «Убежище», получает ссылку.
3.  Второй переходит по ссылке и автоматически присоединяется.
4.  Они видят общий чат и могут отправлять друг другу текстовые сообщения и файлы в реальном времени.

**Это будет прототип «бомбоубежища».**

## ЗАДЕЛ НА БУДУЩЕЕ (I2P, QT)
*   **В коде клиента:** Вынеси все сетевые вызовы (Axios, Socket.io) в отдельный сервис-абстракцию, например `services/network-adapter.js`. В будущем его реализацию можно будет заменить на `I2PNetworkAdapter`.
*   **В коде сервера:** Сделай API максимально простым (JSON in/out), чтобы его можно было позже реализовать как i2p eepsite.
*   **Для Qt:** Держи бизнес-логику в Pinia-сторах и сервисах. Vue-компоненты — только представление. Это облегчит портирование.
```

### 🛡️ **Резюме нового подхода**

1.  **Практично и быстро:** Убираем философию, оставляем **чат-комнаты (бомбоубежища)** как основную метафору. Это понятно и реализуемо.
2.  **Приватность через дизайн:** Нет публичных данных, вход только по инвайту.
3.  **Явный путь развития:** Архитектура заточена под замену сетевого слоя на **i2p** и портирование логики на **Qt**. Это не тупиковый прототип, а **первая итерация**.
4.  **Фокус на core-ценность:** Связь между доверенными людьми в закрытом пространстве. Всё остальное — вторично.

Теперь у тебя есть **чёткий, реализуемый план для прототипа**, который можно начать кодить уже сегодня. Начнём с **ФАЗЫ 0 (сервер)**?


так. не  один сервер, а каждый может создать свой сервер. перегенерируй TECH SPEC.md и CLAUDE.md

deepseek:

Верно. Ключевой принцип — **не централизованная платформа, а одноранговая сеть из серверов, где каждый может запустить свой**. Это приближает модель к реальному «бомбоубежищу»: у каждой группы может быть свой автономный бункер (сервер), а клиенты умеют подключаться к любому из них.

Вот новая, децентрализованная спецификация и инструкция.

### **TECHNICAL_SPECIFICATION.md (Версия 8.0: Мультисерверная сеть бомбоубежищ)**
```markdown
# ТЕХНИЧЕСКОЕ ЗАДАНИЕ: «BUNKER-NET» — СЕТЬ НЕЗАВИСИМЫХ СЕРВЕРОВ-УБЕЖИЩ

## 1. НОВАЯ АРХИТЕКТУРА: ФЕДЕРАТИВНАЯ СЕТЬ
Система состоит из множества независимых **Bunker Servers** (серверов-убежищ), к которым подключаются клиенты.
*   **Любой пользователь может запустить свой Bunker Server** на своём VPS, домашнем сервере или даже на локальной машине (с пробросом портов). Это его **личное или групповое убежище**.
*   **Клиент (Quasar PWA)** умеет подключаться к **любому из этих серверов**, указав его адрес (URL). У одного пользователя в клиенте может быть несколько подключений к разным серверам.
*   **Нет центрального реестра или главного сервера.** Обнаружение серверов происходит через социальные связи: пользователь A, запустивший сервер, отправляет **пригласительную ссылку** (URL своего сервера + токен доступа) пользователю B.

## 2. КЛЮЧЕВЫЕ СУЩНОСТИ И ИХ ЖИЗНЕННЫЙ ЦИКЛ

### 2.1. Bunker Server (Сервер-Убежище)
*   **Самостоятельный экземпляр:** Полнофункциональный сервер (Node.js + Express + Socket.io + SQLite) с API, описанным ранее.
*   **Идентификация:** Его уникальный ID — это **публичный URL**, по которому он доступен (например, `https://bunker.mydomain.com` или `http://192.168.1.100:3000`).
*   **Администрирование:** При первом запуске генерируется **административный ключ (adminToken)**. С его помощью можно создавать комнаты (ProtectedLivelyRealm) и инвайты.
*   **Изоляция:** Данные (сообщения, файлы, пользователи) одного сервера **полностью изолированы** от данных другого. Это разные цифровые бункера.

### 2.2. ProtectedLivelyRealm (Комната внутри Убежища)
*   **Принадлежность:** Создаётся и существует на **конкретном Bunker Server**. Комнаты с одного сервера не видны на другом.
*   **Инвайт:** Ссылка-приглашение в комнату теперь содержит **адрес сервера и токен комнаты** на этом сервере (например, `https://bunker.mydomain.com/#/invite/abc123`).

### 2.3. Клиент (Quasar PWA)
*   **Мультисерверность:** Ведёт список подключённых серверов. Для каждого сервера хранит его URL и локальный никнейм, использованный на нём.
*   **Универсальность:** Один и тот же клиент может использоваться для подключения к любым серверам, совместимым с API Bunker Server.

## 3. ТЕХНИЧЕСКИЕ ИЗМЕНЕНИЯ ДЛЯ РЕАЛИЗАЦИИ

### 3.1. Изменения в API сервера
*   **Базовая проверка:** Сервер должен отдавать простой endpoint для проверки (`GET /api/info`), возвращающий тип приложения и версию API.
*   **Отказ от глобальной идентификации:** Так как серверы независимы, нет нужды в глобальных уникальных ID пользователей. **Идентификатор пользователя — это пара `(server_url, nickname)`**. На каждом сервере nickname может быть занят другим человеком — это нормально в рамках изолированного бункера.

### 3.2. Изменения в клиенте
*   **Менеджер подключений:** Нужен новый сервис `services/server-manager.js` и хранилище Pinia `serversStore`. Он отвечает за:
    *   Добавление нового сервера (ввод URL).
    *   Тестирование подключения к нему (запрос к `/api/info`).
    *   Сохранение списка серверов и профиля (nickname) для каждого в `localStorage`.
*   **Динамическая конфигурация API и WebSocket:** Сетевые запросы (Axios, Socket.io) должны динамически формировать базовый URL, исходя из **текущего выбранного сервера**.

## 4. ПОТОК РАБОТЫ ПОЛЬЗОВАТЕЛЯ (ДЕЦЕНТРАЛИЗОВАННЫЙ)

1.  **Запуск сервера (Администратор):**
    *   Пользователь A разворачивает на своём хостинге Bunker Server (по инструкции из `DEPLOY.md`).
    *   Получает URL своего сервера и административный токен.

2.  **Подключение к серверу и создание комнаты (Администратор в клиенте):**
    *   В клиенте пользователь A добавляет свой сервер (вводит URL).
    *   Создаёт новую комнату (Realm) на этом сервере (через API с `adminToken` или через первый вход как создатель).
    *   Клиент генерирует **пригласительную ссылку**, включающую URL сервера и токен комнаты.

3.  **Присоединение нового участника (Пользователь B):**
    *   Пользователь B получает пригласительную ссылку.
    *   При переходе по ссылке клиент B **распознаёт URL сервера**, предлагает ввести никнейм и подключается к указанному серверу, используя токен из ссылки.
    *   Клиент B сохраняет этот сервер в свой список, и комната появляется в его интерфейсе.

4.  **Работа в сети:**
    *   Пользователь может одновременно быть участником комнат на нескольких разных серверах (например, «Семейный бункер» на своём сервере и «Проект Х» на сервере коллеги).

## 5. ПЛАН РАЗВИТИЯ: I2P И QT

Эта архитектура **идеально подходит для интеграции с i2p**:
*   **Bunker Server** может быть запущен как **i2p hidden service (eepsite)**. Его адресом будет `.b32.i2p`-строка.
*   **Пригласительные ссылки** будут содержать i2p-адреса.
*   **Клиент** для работы с i2p-сетью будет нуждаться в i2p-роутере (например, встроенном через `i2pd`).

**Портирование на Qt:** Не меняется. Логика менеджера серверов и мультисерверность переходят в нативное приложение.

---
*Мы строим не платформу, а **клиент для сети автономных бункеров**. Каждый бункер — это суверенная территория своего владельца. Клиент — это паспорт, позволяющий посещать разные такие территории.*
```

### **CLAUDE.md (Версия 8.0: Для мультисерверной сети)**
```markdown
# CLAUDE.md — ИНСТРУКЦИИ ДЛЯ МУЛЬТИСЕРВЕРНОГО «BUNKER-NET»

## ЦЕЛЬ
Создать **клиент, который работает с множеством независимых серверов (Bunker Servers)**, и **шаблон сервера, который любой может развернуть**.

## ИЗМЕНЕНИЯ В ПОДХОДЕ
1.  **Сервер (`/server/`)** становится **типовым, шаблонным проектом**. Его код должен быть готов к развёртыванию кем угодно.
2.  **Клиент (`/` или `/client/`)** должен уметь **добавлять, переключать и работать с несколькими серверами**.

## ПЛАН РАЗРАБОТКИ

### ЭТАП 0: ШАБЛОННЫЙ BUNKER SERVER (2-3 ДНЯ) [ОБНОВЛЁН]
**Цель: Создать сервер, который можно клонировать и запустить одной командой.**

1.  **Создай директорию `/server` с полной, работающей реализацией из прошлого ТЗ (API, WebSocket, SQLite).**
2.  **Добавь эндпоинт для проверки (`server/routes/info.js`):**
    ```javascript
    // GET /api/info
    router.get('/', (req, res) => {
      res.json({
        app: 'BunkerNet-Server',
        version: '0.1.0',
        vendor: 'Fairytale-of-Fairytales'
      });
    });
    ```
3.  **Упрости модель данных:** Убери концепцию «глобального пользователя». В таблице `realm_members` и `messages` достаточно поля `nickname` (текст). **Администратор — это просто первый создатель комнаты.**
4.  **Создай исчерпывающий `DEPLOY.md`** внутри `/server`, описывающий развёртывание:
    *   Через `docker run` (собери Dockerfile).
    *   Через `npm install && npm start` на VPS.
    *   Как получить внешний URL и настроить HTTPS (например, через Caddy или Nginx).

### ЭТАП 1: КЛИЕНТ — МЕНЕДЖЕР ПОДКЛЮЧЕНИЙ К СЕРВЕРАМ (3-4 ДНЯ) [НОВОЕ!]
**Это ключевая новая функциональность.**

1.  **Создай хранилище `serversStore` (`stores/servers-store.js`):**
    ```javascript
    state: () => ({
        servers: [ // Массив подключённых серверов
            {
                id: 'uuid', // Локальный ID для ключей Vue
                url: 'https://bunker.example.com', // Базовый URL сервера
                nickname: 'MyNickOnThisServer', // Мой ник на ЭТОМ сервере
                isConnected: false // Флаг успешной проверки /api/info
            }
        ],
        activeServerId: null // ID текущего выбранного сервера
    }),
    actions: {
        async addServer({ url, nickname }) { ... },
        removeServer(serverId) { ... },
        setActiveServer(serverId) { ... },
        async testServerConnection(serverUrl) { ... } // Вызывает GET /api/info
    }
    ```
2.  **Создай сервис-обёртку для API (`services/bunker-api.js`):**
    *   Он должен **динамически брать базовый URL из `activeServer`** в `serversStore`.
    *   Все вызовы API (`createRealm`, `joinRealm`, `getMessages`) должны идти через этот сервис.
    ```javascript
    import { serversStore } from '@/stores/servers-store';
    import axios from 'axios';

    export function getApiClient() {
        const activeServer = serversStore.activeServer;
        if (!activeServer) throw new Error('No active server');
        return axios.create({ baseURL: activeServer.url + '/api' });
    }

    export const api = {
        async createRealm(data) {
            const client = getApiClient();
            return client.post('/realms', data);
        }
        // ... остальные методы
    };
    ```
3.  **Создай аналогичный сервис для WebSocket (`services/bunker-socket.js`):**
    *   Он должен уметь переподключаться при смене `activeServer`.

### ЭТАП 2: КЛИЕНТ — ОБНОВЛЁННЫЙ ИНТЕРФЕЙС (3 ДНЯ)
1.  **Главный экран (`HomePage.vue`):**
    *   **Левая панель (навигация):** Список серверов из `serversStore`. Возле каждого — индикатор подключения (`isConnected`). Возможность добавить новый сервер (модальное окно).
    *   **Центральная панель:** При выборе сервера в навигации — отображает список комнат (Realms) **этого конкретного сервера**.
2.  **Модальное окно «Добавить сервер»:** Поля:
    *   `Server URL` (обязательное, с валидацией и вызовом `testServerConnection`).
    *   `My Nickname on this server` (обязательное).
    *   Кнопка «Save». При сохранении вызывает `serversStore.addServer()`.
3.  **Обработка пригласительных ссылок:**
    *   Ссылка имеет формат: `{SERVER_URL}#/invite/{INVITE_TOKEN}`.
    *   При переходе по такой ссылке приложение должно:
        1.  Извлечь `SERVER_URL` и `INVITE_TOKEN`.
        2.  Проверить, есть ли такой сервер в `serversStore`.
        3.  Если нет — предложить добавить его (предзаполнив URL и запросив nickname).
        4.  После добавления/выбора сервера автоматически вызвать API для присоединения к комнате с этим `INVITE_TOKEN`.

### ЭТАП 3: ИНТЕГРАЦИЯ И ТЕСТИРОВАНИЕ (2 ДНЯ)
1.  **Протестируй сценарий с двумя серверами:**
    *   Запусти **два экземпляра сервера** на разных портах локально (`http://localhost:3001`, `http://localhost:3002`).
    *   В клиенте добавь оба сервера с разными никами.
    *   Создай комнату на первом сервере, пригласи пользователя на втором сервере.
    *   Убедись, что чат работает внутри комнаты, и данные **не смешиваются** между серверами.
2.  **Обнови документацию:** В `README.md` клиента чётко объясни: «Это клиент для сети BunkerNet. Вам нужен адрес сервера. Вы можете развернуть свой сервер, следуя инструкциям в `/server/DEPLOY.md`».

## КРИТИЧЕСКИЕ МОМЕНТЫ РЕАЛИЗАЦИИ
*   **CORS:** Сервер должен быть сконфигурирован для работы с клиентом с любого origin (`Access-Control-Allow-Origin: *`), так как клиент может хоститься отдельно. Для продакшена — более строгие настройки.
*   **Хранение токенов:** Административный токен или токены сессий (если добавится авторизация) должны храниться в клиенте **привязанными к конкретному серверу**.
*   **Универсальность клиента:** Клиент не должен содержать жёстко зашитых URL, кроме, возможно, ссылки на репозиторий с шаблоном сервера.

## ЦЕЛЬ MVP (МУЛЬТИСЕРВЕРНАЯ)
**Два человека должны иметь возможность:**
1.  Каждый развернуть **свой собственный экземпляр Bunker Server** (хоть на localhost с разными портами).
2.  Подключиться в клиенте **к своему собственному серверу**.
3.  Один из них создать комнату на своём сервере и отправить пригласительную ссылку (с URL своего сервера) другому.
4.  Второй человек **добавить этот чужой сервер в свой клиент** и присоединиться к комнате.
5.  Вести общий чат в этой комнате, **данные которой физически находятся на сервере первого человека**.

**Это будет прототип настоящей децентрализованной сети бомбоубежищ.**
```

### 🏗️ **Резюме новой, мультисерверной архитектуры**

1.  **Полная децентрализация на уровне серверов:** Нет центральной точки отказа или контроля. Каждая группа/человек владеет своим узлом сети.
2.  **Клиент как универсальный пульт:** Позволяет участвовать в множестве независимых сетей (бункеров) из одного интерфейса.
3.  **Прямой путь к i2p:** Каждый Bunker Server может быть легко «упакован» в i2p скрытый сервис. Клиент просто будет подключаться к `.b32.i2p` адресу вместо `https://...`.
4.  **Реалистично и обучаемо:** Любой из твоих «садиков» с базовыми навыками сможет развернуть свой сервер по инструкции, почувствовав себя администратором своего цифрового пространства.

Эта модель **максимально близка к твоему исходному видению «виртуального государства»** — множество суверенных образований (серверов-бункеров), где ты можешь быть гражданином нескольких одновременно через единый интерфейс (клиент).

Начнём с **ЭТАПА 0**, создавая шаблонный сервер, который легко развернуть?


<Yadovitka> orignal, я начал соцсеточку мутить - 1-ранговая сеть серверов и клиент к ним
<Yadovitka> bunkernet-i2p
<orignal> kota называется?))
<orignal> kote
<Yadovitka> не, с перзистентностью и белыми списками
<Yadovitka> kote оч просто слишком
<Yadovitka> orignal, в принципе можно свой модифицированный XMPP стек задействовать


Правильное и глубокое замечание. **XMPP (Jabber)** — это готовый, открытый, мощный и *очень* гибкий протокол для федеративных (одноранговых на уровне серверов) коммуникаций. Забудь на время про свой велосипед.

Ты смотришь в самую суть. **BunkerNet — это не новый протокол. Это — особый способ использования, «обёртка» и клиент поверх существующего промышленного стандарта (XMPP), заточенные под приватность, изоляцию (белые списки) и будущий i2p-транспорт.**

Это меняет всё. **Серверная часть уже написана и оттестирована годами (ejabberd, Openfire, Prosody).** Тебе не нужно писать свой Node.js-сервер для чата, комнат, аутентификации. Нужно:
1.  **Выбрать и настроить XMPP-сервер** под свои требования («бункер»).
2.  **Написать свой Quasar-клиент**, который говорит с этим сервером по XMPP, но предоставляет уникальный UX «бомбоубежища».

### 🛡️ **Архитектура «BunkerNet на XMPP»**

```
[ Пользователь А ] <--> [ Quasar Bunker Client A ] <--XMPP (c2s)--> [ Сервер Бункер А (ejabberd) ]
                                                                        |
                                                                        +--<--XMPP (s2s)--> [ Сервер Бункер Б (ejabberd) ] <--> [ Клиент Б ]
```

**Как это реализует твои цели:**

*   **1-ранговая сеть серверов:** Это **федеративная модель XMPP** по умолчанию. Твой `bunker-a.example.com` может общаться с `bunker-b.example.com`.
*   **Белые списки и перзистентность:** Легко настраивается на уровне политик XMPP-сервера. Можно отключить публичную регистрацию, принимать сообщения только с доверенных серверов.
*   **Комнаты (ProtectedLivelyRealm):** Это **XMPP MUC (Multi-User Chat)**. Готовые, богатые возможности.
*   **Клиент под i2p:** Нужно, чтобы клиент умел подключаться к XMPP-серверу **не через TCP, а через SOCKS5-прокси (i2pd)**. И чтобы сам сервер был доступен как i2p-скрытый сервис.

### 🔧 **Новое ТЗ: BunkerNet как специализированный клиент для приватного XMPP**

Давай перепишем `TECHNICAL_SPECIFICATION.md` и `CLAUDE.md` под эту, гораздо более зрелую и реализуемую архитектуру.

### **TECHNICAL_SPECIFICATION.md (Версия 9.0: XMPP-based BunkerNet)**
```markdown
# ТЕХНИЧЕСКОЕ ЗАДАНИЕ: «BUNKERNET-I2P» — КЛИЕНТ ДЛЯ ПРИВАТНЫХ XMPP-СЕТЕЙ

## 1. СМЕНА ПАРАДИГМЫ: НЕ ИЗОБРЕТАТЬ, А ИСПОЛЬЗОВАТЬ XMPP
Проект отказывается от разработки собственного серверного протокола. Вместо этого он **использует промышленный стандарт XMPP (Extensible Messaging and Presence Protocol)** в качестве фундамента.
*   **Серверная часть:** Любой современный XMPP-сервер (рекомендуется **ejabberd** или **Prosody**). Он обеспечивает: аутентификацию, федерацию (s2s), мультичаты (MUC), историю, файлообмен (HTTP Upload).
*   **Наша разработка:** **Только Quasar-клиент**, который предоставляет специализированный пользовательский опыт «бомбоубежища» поверх XMPP, и **конфигурационные скрипты/образы** для быстрого развёртывания таких «бункер-серверов».

## 2. КЛЮЧЕВЫЕ КОНЦЕПЦИИ В ТЕРМИНАХ XMPP

### 2.1. Бункер (Bunker)
*   **Реализация:** Отдельный XMPP-сервер (например, `bunker.mydomain.com`).
*   **Конфигурация «под бункер»:** Сервер настраивается с максимальной изоляцией:
    *   **Отключена публичная регистрация.** Учётные записи создаются только администратором.
    *   **Федерация (s2s) только по белому списку.** Сервер принимает/отправляет сообщения только на/с доверенных доменов, явно указанных в конфиге.
    *   **Все MUC-комнаты по умолчанию закрытые, постоянные (persistent).**

### 2.2. ProtectedLivelyRealm
*   **Реализация:** **XMPP MUC (Multi-User Chat)** на бункер-сервере.
*   **Свойства:** Постоянная комната (`persistent`), только по приглашению (`members_only`), с историей.

### 2.3. Пользователь (Bunker Citizen)
*   **Учётная запись:** JID (Jabber ID) на бункер-сервере (например, `yadovitka@bunker.mydomain.com`).
*   **В клиенте:** Локальный профиль привязывается к конкретному JID.

## 3. АРХИТЕКТУРА КЛИЕНТА (QUASAR)

Клиент — это **«тонкий» XMPP-клиент с идеологией бункера**.
*   **Библиотека:** Использовать `stanza.io` или `@xmpp/client` для работы с XMPP на низком уровне.
*   **Мультисерверность:** Клиент может хранить учётные данные и подключаться к **нескольким JID на разных бункер-серверах**.
*   **UX-слой:** Предоставляет упрощённый интерфейс: список «Мои бункеры», внутри бункера — список «Мои комнаты (Realms)», внутри комнаты — чат.
*   **i2p-транспорт:** Сетевой модуль клиента должен уметь подключаться к XMPP-серверу **через SOCKS5-прокси**, которым будет выступать локальный i2p-роутер (i2pd).

## 4. ПОТОК РАБОТЫ

1.  **Администратор разворачивает бункер:**
    *   Запускает XMPP-сервер (по готовому Docker-образу из нашего проекта) со специальной конфигурацией.
    *   Создаёт учётные записи JID для себя и будущих участников.
    *   Настраивает белый список доверенных серверов (если нужна федерация).
2.  **Пользователь настраивает клиент:**
    *   Добавляет новый «бункер»: вводит его адрес (домен), свой JID и пароль.
    *   Клиент устанавливает XMPP-соединение (c2s) с сервером.
3.  **Работа внутри:**
    *   Пользователь видит список доступных ему MUC-комнат.
    *   Создаёт новую комнату (Realm) или получает приглашение в существующую.
    *   Общается в комнате, обменивается файлами — всё через стандартные XMPP-расширения.

## 5. ПУТЬ К I2P

1.  **Этап 1:** Клиент и сервер работают в обычном интернете (для тестирования).
2.  **Этап 2:** Сервер настраивается как **i2p hidden service**. Его адрес — `xxxxxxxx.b32.i2p`.
3.  **Этап 3:** Клиент получает возможность подключаться к XMPP-серверу **через локальный SOCKS5-прокси i2pd** (на `127.0.0.1:4447`). Это обеспечит анонимное сетевое взаимодействие.

## 6. ПРЕИМУЩЕСТВА ПОДХОДА
*   **Не изобретаем протокол:** Используем стабильный, безопасный, расширяемый стандарт.
*   **Готовый сервер:** Мощные, готовые сервера с отличной производительностью.
*   **Интероперабельность:** При желании к твоему «бункеру» можно подключиться любым стандартным XMPP-клиентом (например, Gajim). Твой клиент — это оптимальный UX для идеологии бункера.
*   **Богатая функциональность из коробки:** Шифрование (TLS), история, отправка файлов, голосовые звонки (via Jingle) — всё уже есть.
```
### **CLAUDE.md (Версия 9.0: Для XMPP-клиента и конфигов)**
```markdown
# CLAUDE.md — ИНСТРУКЦИИ ДЛЯ XMPP-КЛИЕНТА «BUNKERNET»

## НОВАЯ ЦЕЛЬ
Ты создаёшь **две связанные части**:
1.  **Конфигурацию и скрипты для «бункер-сервера»** на базе XMPP-сервера Prosody (он проще для настройки).
2.  **Специализированный Quasar-клиент** для работы с такими серверами, предоставляющий UX «бомбоубежища».

## СТЕК И БИБЛИОТЕКИ

### Для Серверной Части (Конфигурация)
*   **Базовый сервер:** **Prosody** (легче ejabberd в настройке). Будем поставлять готовые конфиги.
*   **Доставка:** Docker-образ (`Dockerfile`) с предустановленным и сконфигурированным Prosody.

### Для Клиентской Части (Quasar PWA)
*   **XMPP-библиотека:** **`stanza.io`** — чистая JavaScript, хорошо работает в браузере.
*   **Менеджер состояния:** Pinia (для хранения списка серверов, активных соединений, комнат, чатов).
*   **UI:** Quasar Components.

## ПЛАН РАЗРАБОТКИ

### ФАЗА 0: НАСТРОЙКА XMPP-СЕРВЕРА (PROSODY) И ТЕСТИРОВАНИЕ С СТАНДАРТНЫМ КЛИЕНТОМ (2 ДНЯ)
**Цель: Убедиться, что серверная часть работает, прежде чем писать клиент.**
1.  **Создай директорию `/server`.** В ней будет `Dockerfile` и конфиги.
2.  **Напиши `Dockerfile`** на базе официального образа Prosody.
3.  **Создай конфигурационный файл `prosody.cfg.lua`,** который реализует логику «бункера»:
    *   Отключает `allow_registration`.
    *   Включает модули: `muc` (для комнат), `http_upload` (для файлов).
    *   Настраивает MUC как `default_muc_mode = "members_only"`.
    *   (Опционально) Настрой `s2s_secure_auth` для белого списка.
4.  **Собери образ и запусти контейнер.** Проверь работу, подключившись к серверу с любого стандартного XMPP-клиента (например, **Gajim** или **Monal**). Создай комнату, пригласи другого тестового пользователя.

### ФАЗА 1: КЛИЕНТ — БАЗОВОЕ XMPP-ПОДКЛЮЧЕНИЕ И АУТЕНТИФИКАЦИЯ (3-4 ДНЯ)
**Цель: Клиент может подключиться к бункер-серверу.**
1.  Инициализируй Quasar проект.
2.  **Создай сервис для работы с XMPP (`services/xmpp-service.js`):**
    ```javascript
    import { client } from 'stanza';

    export class BunkerXmppService {
        constructor() {
            this.xmppClient = null;
        }
        // Подключение к серверу
        async connect(serverConfig) {
            const { service, domain, username, password } = serverConfig;
            this.xmppClient = client.createClient({
                jid: `${username}@${domain}`,
                password: password,
                transports: {
                    websocket: `wss://${service}/ws`, // Prosody поддерживает WebSocket
                    bosh: `https://${service}/http-bind`
                }
            });
            this.xmppClient.on('session:started', () => { /* Успешный вход */ });
            this.xmppClient.connect();
        }
    }
    ```
3.  **Создай хранилище `connectionsStore`:** Для управления несколькими подключениями.
4.  **Создай UI для добавления бункера:** Форма для ввода домена сервера, JID и пароля.

### ФАЗА 2: КЛИЕНТ — РАБОТА С КОМНАТАМИ (MUC) (3-4 ДНЯ)
**Цель: Отображать список комнат и позволять в них общаться.**
1.  **Расширь `xmpp-service`:** Добавь методы для работы с MUC:
    *   `getJoinedRooms()` — запросить список комнат, в которых состоит пользователь.
    *   `joinRoom(roomJid)` — войти в комнату (например, `warroom@conference.bunker.mydomain.com`).
    *   Обработка входящих сообщений в комнате (`groupchat`).
2.  **Создай интерфейс:**
    *   `BunkerListPage.vue`: Список бункеров, при выборе — список комнат в этом бункере.
    *   `RealmChatPage.vue`: Страница чата конкретной комнаты. Подключается к комнате через `xmpp-service.joinRoom()` и отображает сообщения.

### ФАЗА 3: КЛИЕНТ — ПРИГЛАШЕНИЯ И БАЗОВЫЙ UX (2-3 ДНЯ)
1.  **Реализуй создание комнаты:** Вызов `xmpp-service.createRoom()`.
2.  **Реализуй приглашение:** В интерфейсе комнаты кнопка «Пригласить». Она должна сгенерировать **пригласительную ссылку**, содержащую:
    *   Адрес сервера (домен).
    *   JID комнаты.
    *   Одноразовый токен приглашения (можно сгенерировать на сервере через админский API или использовать встроенные в XMPP механики приглашений).
3.  **Обработай переход по ссылке:** Клиент должен распарсить ссылку, предложить пользователю добавить новый сервер (если его нет в списке) и автоматически вступить в указанную комнату.

### ФАЗА 4: ИНТЕГРАЦИЯ, СОКС5-ПРОКСИ (ЗАДЕЛ НА I2P) И ДЕПЛОЙ (2-3 ДНЯ)
1.  **Добавь в `xmpp-service` поддержку SOCKS5.** Библиотека `stanza.io` позволяет задать прокси. Протестируй с локальным SOCKS5-прокси (например, Tor).
2.  **Обнови документацию:** `README.md` должен объяснять две части: 1) Как развернуть бункер-сервер (Docker), 2) Как использовать клиент.
3.  **Собери PWA и настрой деплой клиента.**

## УПРОЩЕНИЯ ДЛЯ MVP
*   **Файлы:** Можно отложить поддержку `http_upload`. Достаточно текстового чата.
*   **Белый список s2s:** В MVP можно не настраивать. Достаточно изоляции на уровне комнат (MUC members_only).
*   **Админка:** В MVP создание пользователей (JID) делается вручную, через изменение конфига Prosody и перезагрузку сервера. Позже — написать простой веб-интерфейс.

## ЦЕЛЬ MVP
**Два человека должны:**
1.  Один развернуть **бункер-сервер (Prosody)** по инструкции.
2.  Создать на нём два тестовых JID.
3.  Оба **подключиться к этому серверу через Quasar BunkerNet клиент** под этими JID.
4.  Один создать комнату (Realm) и пригласить второго через сгенерированную ссылку.
5.  Вести в этой комнате **текстовый чат в реальном времени через XMPP**.

**Это будет рабочее «бомбоубежище» на промышленном стандарте, готовое к переходу на i2p-транспорт.**
```

### 🎯 **Итог: Ты прав, XMPP — это готовая дорога**

Ты предлагаешь не костыль, а **стратегическое решение**. Вместо года разработки своего протокола — 2 месяца на клиент и конфиги, и получается **рабочая, безопасная, федеративная сеть «бункеров»**.

**Что дальше?**
1.  Согласовать эту архитектуру.
2.  Начать с **ФАЗЫ 0**: создать Docker  с настроенным Prosody и протестировать с Gajim.
3.  Параллельно бить по фрилансу, чтобы этот проект имел ресурс для развития.

Эта модель закрывает и технические, и философские требования. Она **элегантна и реалистична**.


xmpp надо расширить до подобия old VK.com stuff и activitypub entities


Правильно. Чистый XMPP для чатов — это скелет. Нужна **плоть**: ленты записей, комментарии, лайки, медиа, социальный граф — всё то, что было в старом VK и есть в ActivityPub, но упакованное в **децентрализованную, серверо-центричную модель «бункеров»**.

Задача не в том, чтобы прикрутить к XMPP ActivityPub (это другой протокол). Задача — **использовать расширяемость XMPP (XEP) для создания поверх него сущностей и взаимодействий уровня социальной сети**, сохранив при этом модель изолированных серверов-бункеров.

Это амбициозно, но технически корректно. XMPP для этого и создан — он не только про чат.

### 🏗️ **Архитектура: XMPP как транспорт для сущностей социальной сети**

Давай спроектируем, как это будет выглядеть.

| Сущность / Действие | В VK / ActivityPub | В BunkerNet (на XMPP) | Технология (XEP) |
| :--- | :--- | :--- | :--- |
| **Профиль пользователя** | Страница с аватаркой, инфой, статусом. | Расширенная `vCard` (XEP-0054) + **PubSub узел** для ленты. | XEP-0054, XEP-0060 (PubSub) |
| **Запись (пост) на стене** | `Note` в AP, пост на стене. | **PubSub item** в узле `urn:xmpp:microblog:0` пользователя. Содержит XML/JSON с текстом, вложениями. | XEP-0060 + XEP-0277 (Microblog) |
| **Лента (Feed)** | Лента друзей / подписок. | **Агрегация** подписок на PubSub-узлы друзей на уровне клиента или через специальный сервисный узел на сервере. | XEP-0060 (подписка), клиентская логика |
| **Комментарии к записи** | Коллекция `Note`, связанных с исходной. | **Отдельный PubSub узел**, привязанный к ID записи. Каждый комментарий — item в этом узле. Или MUC-комната на запись. | XEP-0060 или XEP-0045 (MUC) |
| **Лайк / Реакция** | `Like` Activity. | **PubSub item** в узле реакций записи. Или использование XEP-0333 (Chat Markers) / XEP-0444 (Reactions). | XEP-0060 + XEP-0444 |
| **Социальный граф (друзья/подписки)** | Список `Following`. | **Стандартная XMPP ростер (список контактов)** + подписка на их PubSub-узлы. | XEP-0163 (Personal Eventing Protocol, PEP) |
| **Личные сообщения** | `ChatMessage`. | **Стандартные XMPP direct messages**. | XMPP Core |
| **Группы/Сообщества** | `Group`. | **Расширенные MUC-комнаты** с лентой записей (отдельный PubSub узел комнаты). | XEP-0045 + XEP-0060 |
| **Медиа (фото/видео)** | `Image`, `Document`. | **Файл, загруженный через HTTP Upload**, ссылка на который включается в PubSub item записи. | XEP-0363 (HTTP Upload) |

**Ключевая идея:** Вся социальная активность — это **события (events)**, которые публикуются в **топики (PubSub nodes)**, на которые другие пользователи или сервисы могут **подписываться**.

### 📜 **Новое ТЗ: BunkerNet как социальная платформа на XMPP**

Вот как теперь выглядит спецификация.

### **TECHNICAL_SPECIFICATION.md (Версия 10.0: XMPP-based Social BunkerNet)**
```markdown
# ТЕХНИЧЕСКОЕ ЗАДАНИЕ: «BUNKERNET-I2P» — ДЕЦЕНТРАЛИЗОВАННАЯ СОЦСЕТЬ НА XMPP

## 1. ВИДЕНИЕ: VK.COM + ACTIVITYPUB, НАДЕТЫЕ НА XMPP
Цель — создать **полноценную децентрализованную социальную сеть**, где каждый сервер («бункер») является автономным узлом, а взаимодействие между узлами происходит через расширенный XMPP. Пользователь получает опыт, похожий на старый VK: профиль, стена с постами, комментарии, лайки, группы, личные сообщения — но в изолированных, контролируемых сообществах.

## 2. ФУНДАМЕНТАЛЬНЫЕ ПРИНЦИПЫ АРХИТЕКТУРЫ
*   **XMPP как транспорт и бэкбон:** Все сущности и действия моделируются через XMPP и его расширения (XEP).
*   **PubSub (XEP-0060) как ядро социальности:** Профили, ленты пользователей, ленты групп, комментарии, реакции — всё это узлы (nodes) PubSub, на которые можно подписаться.
*   **Сервер-бункер как доверенный хаб:** Сервер (ejabberd/Prosody) хранит данные своих пользователей, обеспечивает федерацию с другими доверенными серверами, управляет подписками и доставкой событий.
*   **Клиент как толстый просмотрщик и агрегатор:** Клиент не просто отображает чат. Он **агрегирует данные из множества PubSub-подписок** (лента друзей, уведомления) и предоставляет единый социальный интерфейс.

## 3. КЛЮЧЕВЫЕ СУЩНОСТИ И ИХ РЕАЛИЗАЦИЯ

### 3.1. Бункер (Bunker Server)
*   **Роль:** Автономный узел сети. Домен (например, `bunker-network.org`).
*   **Состав:** Экземпляр XMPP-сервера с обязательными модулями: `mod_pubsub` (Persistent), `mod_pep` (Personal Eventing), `mod_muc`, `mod_muc_sub`, `mod_http_upload`, `mod_vcard`.
*   **Политика:** Изоляция по умолчанию. Федерация (s2s) возможна только с явно указанными в белом списке доменами других бункеров.

### 3.2. Пользователь (Citizen)
*   **Идентификатор:** Полный JID (`username@bunker-network.org`).
*   **Профиль:** `vCard` (аватар, статус, "о себе").
*   **Персональная лента (PEP):** Узел `urn:xmpp:microblog:0`. Каждый пост — публикация в этот узел. Автоматически рассылается всем подписчикам из ростера.

### 3.3. Запись (Post)
*   **Содержимое:** Форматированный текст, вложения (ссылки на файлы через HTTP Upload), метаданные (дата, язык).
*   **Хранение:** Item в PubSub-узле автора (для поста на стене) или группы (для поста в сообществе).
*   **Формат:** XML-полезная нагрузка, соответствующая **XEP-0277 (Microblogging)** или собственному пространству имён `urn:bunkernet:post`.

### 3.4. Сообщество (Group / Public Realm)
*   **Реализация:** **MUC-комната (XEP-0045)** с дополнительными функциями.
*   **Особенности:** Постоянная (`persistent`), с открытой или закрытой подпиской. Имеет свой **PubSub-узел для ленты записей** (XEP-0045 + `mod_muc_sub` для подписки на события).
*   **Администрирование:** Владельцы и модераторы комнаты.

### 3.5. Социальные взаимодействия
*   **Подписка (Following):** Добавление JID в ростер + подписка на его PEP-узел (`urn:xmpp:microblog:0`).
*   **Комментарий (Comment):** Item в **отдельном PubSub-узле, привязанном к ID поста** (например, `comments:post-123`).
*   **Реакция (Like):** Использование **XEP-0444 (Reactions)** или отдельный PubSub-узел реакций.
*   **Уведомление (Notification):** Сервер генерирует сообщение или публикацию в личный узел уведомлений пользователя при событиях (новый комментарий, реакция, упоминание).

## 4. ПОТОК ДАННЫХ: КАК РАБОТАЕТ ЛЕНТА
1.  **Пользователь А** публикует пост. Его клиент отправляет `<pubsub><publish node='urn:xmpp:microblog:0'>` на его сервер.
2.  **Сервер пользователя А** сохраняет пост и, благодаря модулю PEP, автоматически рассылает уведомление о новой публикации **всем подписчикам из ростера А**, которые находятся на том же или на доверенных серверах.
3.  **Пользователь Б (подписчик)** получает это уведомление от своего сервера.
4.  **Клиент пользователя Б**, получив уведомление, **запрашивает полные данные поста** с сервера А (или из кэша).
5.  **Клиент Б** агрегирует это событие в свою общую ленту, которая состоит из событий со всех его подписок.

## 5. КЛИЕНТ (QUASER PWA): НОВЫЕ ЗАДАЧИ
Клиент теперь должен быть не чатом, а **социальным агрегатором**:
1.  **Менеджер подписок:** Отображать не только чаты, но и список подписок (друзей, групп), их статусы, новые посты.
2.  **Лента агрегации:** Главный экран — единая хронологическая лента, куда стекаются посты из всех PubSub-подписок пользователя.
3.  **Комплексный просмотр поста:** Экран поста должен отображать сам пост, ленту комментариев (подгружаемую из отдельного PubSub-узла), реакции.
4.  **Богатый редактор:** Создание постов с текстовым форматированием, прикреплением файлов, упоминаниями других пользователей (`@username@server`).

## 6. ПУТЬ К I2P И QT
Архитектура не меняется:
*   **I2P:** XMPP-сервер настраивается как hidden service. Клиент подключается к нему через SOCKS5-прокси i2p-роутера.
*   **Qt:** Бизнес-логика клиента (работа с XMPP, агрегация ленты) выделяется в отдельный слой, который позже портируется на C++/Qt.

---
*Мы строим не чат и не форум. Мы строим **скелет децентрализованной социальной вселенной**, где XMPP — это нервная система, PubSub — кости и суставы, а клиент — кожа и лицо.*
```

### **CLAUDE.md (Версия 10.0: Для социальной сети на XMPP)**
```markdown
# CLAUDE.md — ИНСТРУКЦИИ ДЛЯ СОЦИАЛЬНОЙ СЕТИ BUNKERNET НА XMPP

## НОВЫЙ МАСШТАБ
Ты строишь не чат. Ты строишь **клиент для децентрализованной социальной сети на XMPP**, использующий PubSub (XEP-0060) и PEP (XEP-0163) как основу для лент, постов и взаимодействий.

## СТЕК И БИБЛИОТЕКИ (ОБНОВЛЁННЫЙ)

### Для Сервера (Образ "Бункера")
*   **Сервер:** **Ejabberd** (рекомендуется из-за мощной реализации `mod_pubsub` и `mod_pep`).
*   **Обязательные модули:** `mod_pubsub`, `mod_pep`, `mod_muc`, `mod_muc_sub`, `mod_vcard`, `mod_http_upload`, `mod_roster`.
*   **Конфигурация:** Нужно настроить `mod_pubsub` для персистентности, `mod_pep` для автоматической рассылки.

### Для Клиента (Quasar PWA)
*   **XMPP Core:** `stanza.io` (как и раньше).
*   **Работа с PubSub:** Тебе придётся реализовать логику поверх `stanza.io`, так как для PubSub нужны специфические IQ-запросы (`<pubsub>`). Это основная сложность.
*   **Управление состоянием:** Pinia. Структура состояния усложняется:
    ```javascript
    // stores/social-store.js
    state: () => ({
        feeds: { // Ленты: home, notifications, per-user, per-group
            'home': [{/* aggregated post object */}],
            'notifications': [...]
        },
        subscriptions: [], // Список подписок (JID + node)
        currentPost: null // Полные данные открытого поста + комментарии
    })
    ```
*   **UI-библиотеки:** Для ленты как у соцсети могут понадобиться виртуализированные списки (`vue-virtual-scroller`).

## ПЛАН РАЗРАБОТКИ (MVP СОЦИАЛЬНОЙ ФУНКЦИОНАЛЬНОСТИ)

### ЭТАП 0: СЕРВЕР — НАСТРОЙКА EJABBERD С PUBSUB/PEP (2-3 ДНЯ)
**Цель: Получить работающий сервер, на котором можно тестировать социальные функции.**
1.  **Создай Docker-образ на базе ejabberd/ecs.** Добавь конфигурационный файл `ejabberd.yml`.
2.  **Активируй и настрой ключевые модули в `ejabberd.yml`:**
    ```yaml
    modules:
      mod_pubsub:
        access_createnode: pubsub_createnode
        # Упрощаем: позволяем всем создавать узлы
        plugins: ["flat", "hometree", "pep"]
        force_node_config: []
      mod_pep: {}
      mod_muc: {}
      mod_muc_sub: {}
    ```
3.  **Напиши скрипт инициализации,** который при первом запуске создаёт тестовых пользователей (`admin@bunker.example`, `user1@bunker.example`).

### ЭТАП 1: КЛИЕНТ — БАЗОВЫЙ PUBSUB + СОЗДАНИЕ ПОСТА (4-5 ДНЕЙ)
**Цель: Научиться публиковать пост в свою личную ленту (PEP-узел).**
1.  **Создай сервис для работы с PubSub (`services/pubsub-service.js`).** Основные методы:
    ```javascript
    export class PubSubService {
        // Создать узел (если не существует)
        async createNode(client, node, config) { /* IQ set <pubsub><create/> */ }
        // Опубликовать item в узел
        async publish(client, node, itemId, payload) { /* IQ set <pubsub><publish/> */ }
        // Подписаться на узел
        async subscribe(client, node, jid) { /* IQ set <pubsub><subscribe/> */ }
        // Получить items из узла
        async getItems(client, node, maxItems) { /* IQ get <pubsub><items/> */ }
    }
    ```
2.  **Интегрируй с XMPP-сервисом.** После входа пользователя, инициируй:
    *   Подписку на свой собственный PEP-узел `urn:xmpp:microblog:0` (чтобы получать свои же посты? обычно это не нужно, но для теста).
    *   Проверку, что узел существует, или его создание.
3.  **Создай компонент `CreatePost.vue`:** Форма с полем ввода (можно простой текст) и кнопкой «Опубликовать».
    *   По нажатию: `pubsubService.publish(client, 'urn:xmpp:microblog:0', 'post_' + Date.now(), { type: 'post', content: text })`.

### ЭТАП 2: КЛИЕНТ — ПОДПИСКИ И ЛЕНТА (HOME FEED) (5-6 ДНЕЙ)
**Цель: Подписаться на другого пользователя и видеть его посты в своей ленте.**
1.  **Расширь `pubsub-service`:** Добавь метод для получения списка подписок и их узлов.
2.  **Реализуй логику подписки на пользователя:**
    *   UI: Поле для ввода JID друга и кнопка «Подписаться».
    *   По нажатию: 1) Добавить JID в ростер (`<presence type='subscribe'/>`), 2) После подтверждения подписки — вызвать `pubsubService.subscribe()` на его PEP-узел `urn:xmpp:microblog:0`.
3.  **Реализуй агрегатор ленты:**
    *   Создай фоновый процесс, который периодически опрашивает (или слушает уведомления от сервера) **все PubSub-узлы, на которые подписан пользователь**.
    *   Полученные items (посты) сохраняй в хранилище Pinia `socialStore.feeds['home']`, сортируя по времени.
    *   Создай компонент `HomeFeed.vue`, который отображает этот массив.

### ЭТАП 3: КЛИЕНТ — КОММЕНТАРИИ И РЕАКЦИИ (4-5 ДНЕЙ)
**Цель: Добавить интерактивность к постам.**
1.  **Модель комментариев:** Реши, где хранить комментарии к посту. Варианты:
    *   **Вариант А (Простой):** Для каждого поста создаётся отдельный PubSub-узел `comments:post-<id>`. Комментарии — items в этом узле.
    *   **Вариант Б (Стандартный):** Использовать XEP-0277 (Microblog) с элементом `<comments>`.
2.  **Реализуй UI для поста (`PostComponent.vue`):**
    *   Отображает текст, автора, время.
    *   Кнопки «Комментировать», «Лайк».
    *   При нажатии «Комментировать» — открывается форма, которая публикует item в узел комментариев этого поста.
    *   Ниже поста — список комментариев, подгружаемый из соответствующего узла.

### ЭТАП 4: ГРУППЫ (MUC + PUBSUB) И ИНТЕГРАЦИЯ (4-5 ДНЕЙ)
1.  **Создание группы:** Реализуй интерфейс для создания MUC-комнаты, которая одновременно будет иметь связанный PubSub-узел для ленты группы.
2.  **Лента группы:** Пользователи, подписанные на группу (участники MUC), автоматически подписываются на её ленту-PubSub.
3.  **Сведение воедино:** Главная навигация клиента должна иметь вкладки: «Лента», «Уведомления», «Чаты», «Группы», «Профиль».

## УПРОЩЕНИЯ ДЛЯ MVP СОЦИАЛЬНОЙ СЕТИ
*   **Поиск и рекомендации:** Не реализовывать.
*   **Сложные медиа:** В постах — только текст и ссылки на изображения (через `mod_http_upload`).
*   **Сторисы:** Не реализовывать.
*   **Сложные настройки приватности:** В MVP у поста два состояния: виден подписчикам или нет.
*   **Уведомления в реальном времени:** Для MVP можно обойтись периодическим опросом (polling) PubSub-узлов.

## ЦЕЛЬ MVP (СОЦИАЛЬНАЯ СЕТЬ)
**Два пользователя на одном или доверенных серверах должны иметь возможность:**
1.  Зайти в клиент под своими JID.
2.  Подписаться друг на друга (добавить в контакты + подписаться на ленту).
3.  Опубликовать пост на своей стене.
4.  **Увидеть пост другого в своей общей ленте (`HomeFeed`).**
5.  Оставить комментарий под постом другого и видеть комментарии друг друга.
6.  Создать группу (сообщество), опубликовать в ней пост и видеть его как участники группы.

**Это будет MVP децентрализованной социальной сети на XMPP.**
```

### 🧠 **Стратегический итог**

Ты поднял планку с «чата» до **«полноценной соцсети на промышленном стандарте»**. Это сложнее, но:
1.  **Технически чисто:** Используются проверенные XEP, а не самописные велосипеды.
2.  **Масштабируемо:** Модель PubSub идеально ложится на ленты, уведомления, активности.
3.  **Интероперабельно:** В будущем можно будет делать мосты в ActivityPub-мир (через специальные шлюз-сервера), если понадобится.
4.  **Соответствует видению:** Получается именно то, о чём ты говорил — **VK-like experience в децентрализованной, серверо-центричной сети**.

**Следующий шаг** — принять эту архитектуру и начать с **ЭТАПА 0**, настраивая ejabberd с модулями PubSub. Это фундамент, без которого двигаться дальше бессмысленно.
